# Issues — 2026-08-31 用户反馈

> 本次会话中由用户报告的 3 个产品问题清单。
> 范围：仅记录，不做代码修改。
> 关联代码位置已标出，便于后续修复时定位。

---

## 1. 设置页：内置语音引擎解压阶段仍硬编码显示「正在下载…请保持网络连接」

### 现象
- 在设置页"语音"段下载内置 TTS 模型。
- 下载完成进入"解压"阶段后（_downloadProgress = Extracting(...)），
  - 卡片**内部**进度条下方文字正确显示"解压中 (xxx/yyy) <filename>"（这部分对）。
  - 卡片**底部**的另一个 `SettingRow`（图标 Downloading）仍硬编码显示 **"正在下载..." / "请保持网络连接"**，
  - 整张卡片视觉上同时给用户"在下载"和"在解压"两个互相矛盾的信号。

### 根因（代码定位）
- 文件：`app/src/main/java/com/eareyereading/ui/screens/settings/SettingsScreen.kt`
- 区间：`905-912` 行
- 摘录：
  ```kotlin
  if (uiState.embeddedDownloading || uiState.embeddedInitializing) {
      SettingRow(
          icon = Icons.Default.Downloading,
          iconBg = SurfaceSecondary,
          iconColor = OnSurfaceTertiary,
          title = if (uiState.embeddedInitializing) "正在初始化..." else "正在下载...",
          subtitle = "请保持网络连接",
      )
  }
  ```
- 该 `SettingRow` 与卡片内部 `864-878` 行的 `LinearProgressIndicator` 是**两个独立 UI 组件**，
  各自维护状态。底部 `SettingRow` 没接 `embeddedDownloadStage`，
  也没区分 `EmbeddedTtsEngine.Progress.Extracting` 阶段。

- 顺带影响阅读页弹窗 `TtsInstallDialog`（`ReaderScreen.kt:494-495`）：
  ```kotlin
  text = downloadStage
      ?: "正在下载内置 TTS 模型 ${(progress * 100).toInt()}%…请保持网络，不要关闭应用",
  ```
  fallback 文案是兜底逻辑，理论上 `downloadStage` 不为 null 时不会触发。
  但若 ViewModel collect 阶段切换瞬间 `stage == null`，
  进度条刚切到"解压中"前那一帧会闪过这行"请保持网络"。
  （待确认是否实际可见）

### 期望行为
- 解压阶段：底部 `SettingRow` 的 title 改为"正在解压"或直接隐藏，
  subtitle 改为"解压中…（无需联网）"或留空。
- 初始化阶段：title 改为"正在初始化"，subtitle 改为"无需联网"。
- 整个下载/解压/初始化生命周期：始终与卡片内部进度条文案一致。
- 阅读页弹窗的 fallback 文案：
  改为"下载内置 TTS 模型…请保持网络连接"以**明确阶段**为下载态，
  或者干脆只在确实 Downloading 时用 fallback。

### 涉及文件
- `app/src/main/java/com/eareyereading/ui/screens/settings/SettingsScreen.kt:905-912`
- `app/src/main/java/com/eareyereading/ui/screens/reader/ReaderScreen.kt:494-495`（次要）

### 关联 Bug（同一组件）
详见本文件第 2 节「解压时文件数量统计的预扫分母 bug」。

---

## 2. 内置语音引擎：解压时文件数量统计的预扫分母 bug

### 现象
- 解压 Piper 归档时，进度条可能永远到不了 100%，停滞在 99.x%。
- 体感：解压阶段进度条长时间不动，用户误以为卡死。

### 根因
- 文件：`app/src/main/java/com/eareyereading/tts/EmbeddedTtsEngine.kt`
- 两个函数对"什么算 entry"的口径不一致：
  - `countTarEntries`（851-868 行，预扫）：**所有 entry 都 +1**（不区分 file/dir/其他类型）
  - 解压循环（1007-1012 行）：**只数 `entry.isFile || entry.isDirectory`**，跳过 symlink/fifo 等
- 后果：归档里如果包含 symlink（实际 Piper 的 `espeak-ng-data/phonindex` 等就是 symlink），
  分母（预扫）> 分子（解压），进度永远到不了 1.0。

### 期望行为
- 预扫与解压循环用同一套过滤条件，统计口径一致。
- 建议抽一个 `shouldCountTarEntry(entry, canonicalRoot): Boolean`，
  两个调用点共用，分母分子口径永远一致。

### 详细分析（5 条子 bug）

#### Bug 1（P0，必修）：预扫 / 解压循环过滤口径不一致 → 进度卡 99%

**预扫** (`countTarEntries`, 851-868 行)：
```kotlin
private fun countTarEntries(tarballFile: File): Int {
    var count = 0
    try {
        java.io.FileInputStream(tarballFile).use { fis ->
            BZip2CompressorInputStream(fis).use { bzis ->
                TarArchiveInputStream(bzis).use { tis ->
                    while (tis.nextEntry != null) {
                        count++   // 不分类型，所有 entry 都 +1
                    }
                }
            }
        }
    } catch (e: Exception) { ... return -1 }
    return count
}
```

**解压循环** (1007-1012 行)：
```kotlin
if ((!entry.isFile && !entry.isDirectory) ||
    !File(modelsDir, name).canonicalPath.startsWith(canonicalRoot)
) {
    entry = tis.nextEntry
    continue    // 跳过，非 file/dir 不计入 extractedEntries
}
extractedEntries++
```

**口径差异**：
- 预扫：所有 entry 都 +1（symlink、fifo、char device 等都算）
- 解压：只数 `entry.isFile || entry.isDirectory`，其他类型不计入分子

**后果**：
归档里夹 symlink 时（**实际 Piper `espeak-ng-data/phonindex` 就是指向 `phondata` 的 symlink**），
分母（预扫）> 分子（解压），**进度条永远到不了 1.0**，UI 永远停在 412/415 的 99%。

**修法（推荐）**：抽一个私有函数 `shouldCountTarEntry(entry: TarArchiveEntry, canonicalRoot: String): Boolean`，
把过滤条件集中，预扫跟解压循环都调它，分母分子永远一致。

---

#### Bug 2（P2）：预扫无路径校验（与 Bug 1 配对出现）

`countTarEntries` 没有参数 `modelsDir`，**根本没法做 path traversal 校验**。
解压循环里有的 `canonicalPath.startsWith(canonicalRoot)` 检查，在预扫里**不存在**。

不算安全漏洞（解压那一步是守得住的），但跟 Bug 1 是同一类问题：
不安全的归档里 path traversal 条目**被数到分母、但解压时跳过**，
进一步放大 Bug 1 的进度卡死。

**修法**：把 `modelsDir` 传给 `countTarEntries`，或者抽到工具函数里复用同一个 root 路径。

---

#### Bug 3（P2）：目录条目计入"文件数"，UI 体感偏差

tar 解压常见形态：先一条 `dir entry`（`isDirectory=true`），再零到多条 `file entry` 在它下面。
代码里不管 `isFile` 还是 `isDirectory`，都 `extractedEntries++`（1047 行）。

**用户感知偏差**：
- 预扫得「N 个 entry」 → UI 显示"解压中 100/412" = 24.3%
- 实际 100 个里有 30 个是空目录、70 个是文件
- 再解压 300 个文件，体感上"卡 1/4"那一会儿实际是空目录扫得快，**并不是真的"解压 1/4"**

**修法**：要么只数 `isFile`（贴近"解压文件数"），要么 UI 文案说清楚"包含目录"。
**建议只数 `isFile`**：目录条目通常很短（mkdirs() 一行），
用户对"我刚才在解压 412 个文件"更有耐心，体感更连贯。

---

#### Bug 4（P1）：预扫失败时末态分子分母差 1，UI 不收敛

967-970 行动态分母兜底：
```kotlin
if (preCountedEntries <= 0 && extractedEntries >= totalEntries) {
    totalEntries = extractedEntries + 1
}
```

`pushExtractionProgress` 里每次调用都判断 `extractedEntries >= totalEntries` → 分母 +1。
下一个 entry 解出来 `extractedEntries++` 后**再次**满足条件，分母又 +1。
只要解压还在继续，分母永远比分子大 1 —— 这是设计意图（保持单调前进），没问题。

**但末态不收敛**：解压完最后一条 entry 后，`extractedEntries` 和 `totalEntries` 可能差 1，
UI 永远停在 `412/413` 而不是 `412/412`（在预扫失败路径上）。
下面 1051 行 `pushExtractionProgress(isFinal=true)` 之后没再调 `extractedEntries++`，
**也没把分子推到分母**。

**修法**（1051 行附近）：
```kotlin
pushExtractionProgress(isFinal = true)
// 补一句：预扫失败路径末态对齐
if (preCountedEntries <= 0) {
    _downloadProgress.value = Progress.Extracting(
        extractedEntries, extractedEntries, lastExtractionEntry,
    )
}
```
或者直接把末态的 `extractedEntries / totalEntries` 分子分母都设成一样的值。

---

#### Bug 5（P3）：异常态 UX 跳变

解压某条 entry 的 `FileOutputStream.use {}` 里抛了 `MAX_EXTRACT_BYTES` 触发的 IOException
（1031-1034），这条 entry 可能已经写了**部分**内容到磁盘。
catch (e: Exception) 里调 `cleanExtractionPartials` 清理了，
**但进度流没强制推到 (n, n) 的"已清理"态**。

UI 可能从「解压中 200/412」瞬间切到「Failed」，没一个明确的「已清理」过渡。

不致命，纯 UX 跳变。如果想做得更细，加一个 `Progress.CleaningUp` 中间态。

### 涉及文件
- `app/src/main/java/com/eareyereading/tts/EmbeddedTtsEngine.kt:851-868`（`countTarEntries`）
- `app/src/main/java/com/eareyereading/tts/EmbeddedTtsEngine.kt:967-980`（动态分母兜底）
- `app/src/main/java/com/eareyereading/tts/EmbeddedTtsEngine.kt:1007-1012`（解压循环过滤）
- `app/src/main/java/com/eareyereading/tts/EmbeddedTtsEngine.kt:1045-1052`（末态 push）

---

## 3. 阅读主题 与 外观模块的「深色模式」冲突

### 现象
- 设置页同时存在两套主题控制：
  - **「阅读主题」**（807-831 行，ReadingTheme 枚举：LIGHT / DARK / SEPIA）：控制阅读页背景/文字配色。
  - **「外观 / 深色模式」**（971-978 行，`uiState.darkMode` 开关）：控制整个 App 的 MaterialTheme 主题。
- 两套系统**未联动**：
  - 用户在"外观"里关掉深色模式，App 切到浅色 MaterialTheme；
    但如果之前在"阅读主题"里选了 DARK，阅读页背景仍用 `DarkBg`（硬编码 Color，不跟 MaterialTheme 走）。
  - 反之：用户在"阅读主题"里选 SEPIA，但 App 全局仍是浅色 MaterialTheme，
    弹窗/对话框/设置页列表跟阅读页主题不一致，整体视觉割裂。
- "深色模式"开关打开/关闭，**阅读页背景不变**；用户预期是"App 全深色 + 阅读页深色背景"。

### 根因（代码定位）
- 文件：`app/src/main/java/com/eareyereading/ui/screens/settings/SettingsScreen.kt`
- 阅读主题（ReadingTheme）：
  - 807-831 行：阅读主题 FilterChip 选择器，调用 `viewModel.setTheme(theme)`
  - 设置项定义在"阅读"卡片内
- 外观深色模式（darkMode）：
  - 967-990 行："外观"段，"深色模式" Switch
  - 调 `viewModel.setDarkMode` 写 DataStore `DARK_MODE`
- 阅读页背景渲染（`ReaderScreen.kt:102-111`）：
  ```kotlin
  val backgroundColor = when (uiState.theme) {
      ReadingTheme.LIGHT -> MaterialTheme.colorScheme.background
      ReadingTheme.DARK -> DarkBg         // 硬编码
      ReadingTheme.SEPIA -> SepiaBg       // 硬编码
  }
  ```
  `DarkBg` / `SepiaBg` 是 `ui/theme/Color.kt` 里的硬编码常量，
  与 `MaterialTheme.colorScheme` 是**两套独立色彩系统**。
- MaterialTheme 在 `MainActivity` / `Theme.kt` 里根据 `darkMode` 切换。

### 用户要求
- **直接把"外观"模块（971-990 行的整个"外观"段）移除**。
- App 主题完全由"阅读主题"控制：
  - LIGHT → App 浅色 MaterialTheme + 阅读页浅色
  - DARK → App 深色 MaterialTheme + 阅读页深色
  - SEPIA → App 浅色 MaterialTheme + 阅读页 sepia 背景（保留）
- `darkMode` 字段在 DataStore 里可保留兼容旧数据，但 UI 不再暴露。
- 移除时需注意：
  - `viewModel.setDarkMode` 函数（422 行附近）保留兼容层（写 DataStore 但 UI 不调）
  - 或彻底删除：注意 grep 所有调用方
  - `MainActivity` 里基于 `darkMode` 选 `darkColorScheme`/`lightColorScheme` 的逻辑可改为永远读 ReadingTheme

### 涉及文件
- `app/src/main/java/com/eareyereading/ui/screens/settings/SettingsScreen.kt:965-990`（"外观"段，含深色模式 + Collins 高亮）
- `app/src/main/java/com/eareyereading/ui/screens/settings/SettingsScreen.kt:422-425`（`setDarkMode` 函数）
- `app/src/main/java/com/eareyereading/ui/screens/reader/ReaderScreen.kt:102-111`（阅读页背景渲染）
- `app/src/main/java/com/eareyereading/ui/theme/Theme.kt`（MaterialTheme 切换逻辑）
- `app/src/main/java/com/eareyereading/data/repository/SettingsRepositoryImpl.kt:89-91`（DataStore `DARK_MODE` 写入）

### 关联问题
- 本节移除"外观"段后，**第 4 节"高亮等级"开关也在 980-987 行同一卡片内**，会一并被移除。
  这与用户单独提的"高亮等级直接打开并不可修改"问题（见下）方向一致：
  既然 UI 语义跟 reader 端实际状态不同步，先把 UI 入口砍掉，reader 端另寻更合适的方案。

---

## 4. 高亮 Collins 等级：直接打开并不可修改

### 现象
- 在设置页"外观"段，"高亮 Collins 等级"开关默认**开启**（`collinsHighlight` 默认 `true`）。
- 用户反馈"直接打开并不可修改"：
  - 用户尝试关闭后，进入阅读页词频颜色**仍然显示**（无变化）。
  - 用户尝试重新打开/关闭多次，行为不一致。

### 根因（代码定位）
- 文件：
  - `SettingsScreen.kt:980-987`（Settings 端的"高亮 Collins 等级"开关）
  - `SettingsScreen.kt:56`（`_uiState.collinsHighlight` 默认 `true`）
  - `SettingsScreen.kt:442-444`（`setCollinsHighlight` 写 DataStore）
  - `SettingsRepositoryImpl.kt:83-84, 99-101`（DataStore 读写，默认 `true`）
  - `ReaderViewModel.kt:126`（Reader 的 `showWordLevelColors` 默认 `false`）—— **独立字段，没读 DataStore**
  - `ReaderViewModel.kt:1768-1770`（`toggleWordLevelColors` 只改内存 `showWordLevelColors`，**不写 DataStore**）
  - `ReaderScreen.kt:1930-1936`（阅读页内的"Collins 词频色彩" Switch，调 `toggleWordLevelColors`）

### 状态分裂
- Settings 端：
  - 字段 `collinsHighlight`（DataStore 持久化），UI "高亮 Collins 等级" Switch。
- Reader 端：
  - 字段 `showWordLevelColors`（ReaderViewModel 内存），UI "Collins 词频色彩" Switch。
- **两个字段互不感知**：
  - Settings 开 collinsHighlight=true，进入 Reader，showWordLevelColors 仍是 false（默认），
    词频颜色不显示，用户以为"开关没生效"。
  - Reader 内手动开 showWordLevelColors=true，离开/重启 ReaderViewModel 后回 false，
    用户以为"开关被自动关了"。
  - Reader 内关 showWordLevelColors=false，回到 Settings，collinsHighlight 仍是 true，
    用户以为"关不掉"。

### 期望行为
- 短链：把"高亮 Collins 等级"开关砍掉（参见第 3 节：用户已要求砍掉"外观"段）。
- 中链：合并为单一字段，Reader 端 init 时从 DataStore 读 `collinsHighlight`，
  写时回写 DataStore。
- 等级语义：当前实现只是 on/off，但 Collins 实际有 5 级（核心/进阶/提高/进阶高/罕用）。
  若后续要做"等级选择器"，应改成 1-5 星 Slider / SegmentedButton。

### 用户要求
- **直接把"外观"段（含此开关）移除**。
- Collins 词频颜色的入口保留在阅读页内（"Collins 词频色彩" Switch），
  仍走 ReaderViewModel 内存的 `showWordLevelColors`（不持久化，体验上保持"在阅读页内调"）。
  若后续要持久化，再单独做。

### 涉及文件
- `app/src/main/java/com/eareyereading/ui/screens/settings/SettingsScreen.kt:980-987`（要被移除的 Switch）
- `app/src/main/java/com/eareyereading/ui/screens/settings/SettingsScreen.kt:56, 178-186, 442-444`（相关状态/函数，可保留兼容层）
- `app/src/main/java/com/eareyereading/data/repository/SettingsRepositoryImpl.kt:83-84, 99-101`（DataStore 读写，可保留兼容层）
- `app/src/main/java/com/eareyereading/ui/screens/reader/ReaderScreen.kt:1930-1936`（阅读页内 Switch，保留）
- `app/src/main/java/com/eareyereading/ui/screens/reader/ReaderViewModel.kt:126, 1768-1770`（Reader 端内存开关，保留）

---

## 5. 阅读详情页：屏占比不足 + 操作栏常驻挤压阅读空间

### 现象
- 阅读详情页（`ReaderScreen`）顶部 TopAppBar + 底部 ReadingBottomBar **常驻显示**，
  把可阅读区域挤掉一大截。
- TopAppBar 高度本身约 64dp，叠加系统状态栏（约 24-30dp）后，顶部留白合计 ~90dp。
- BottomBar 含翻页 / 进度条，高度约 56-72dp；叠加系统导航栏后底部留白合计 ~80-100dp。
- 在主流 6.5-6.7 寸手机（屏幕高度 ~880dp）上，可阅读区域只剩 ~700dp，**有效屏占比不足 80%**。
- 长段落（4-5 行）滚动时，操作栏始终占据屏幕上下边缘，**没有任何"沉浸阅读"状态**。
- 用户期待：默认进入阅读态时只看到正文；**轻击空白区域显示/隐藏 TopAppBar + BottomBar**（类似 iBooks / 多看 / 微信读书 的"点击中央切换 chrome"交互）。

### 根因（代码定位）
- 文件：`app/src/main/java/com/eareyereading/ui/screens/reader/ReaderScreen.kt`
- `Scaffold` 始终以 `topBar` + `bottomBar` 形式挂载（113-262 行），没有 chrome 可见性状态。
- `Scaffold` 的 `contentWindowInsets` 没有显式设置，**默认让 TopAppBar/BottomBar 吃满系统状态栏 + 导航栏的高度**，
  又额外增加了一截系统 insets 的不可读区域。
- 段落实体（`LazyColumn`）被 `padding(horizontal = 20.dp)` 包裹（269 行），
  横向 20dp 边距对长段落阅读是合理的；**问题在垂直方向**：`padding(padding)` 用的是 Scaffold 内部 padding，
  等同于 topBar/bottomBar 的固定高度。
- 没有检测"轻击中央空白"的手势或点击监听器。

### 期望行为
1. **默认进入沉浸态**：进入阅读页时 TopAppBar + BottomBar **隐藏**，
   段落实体向上/向下扩展到屏幕真正可用区域。
2. **轻击空白切换 chrome**：
   - 点击段落**之间的空白**（不是段落文字本身，避免误触单词查询）切换 chrome 显隐。
   - 段落文字上的点击仍走现有的"选词 / 选句 / 翻页"逻辑，不受影响。
3. **过渡动画**：chrome 显/隐用 `AnimatedVisibility`（slideInVertically + fadeIn，约 200ms），
   不要瞬切。
4. **保留交互场景的 chrome**：以下场景应**始终显示** chrome 或显式重置为显示：
   - 滚动 LazyColumn 时短暂显示（scroll-driven reveal）。
   - 弹出选词卡片 / 翻译弹层 / 设置抽屉时必须显示（避免被遮后无法操作）。
   - TTS 播放中显示 BottomBar（方便用户随时暂停/快进）。
5. **屏占比优化**（与 chrome 隐藏独立）：
   - 段落实体加 `contentPadding` 而不是 `Modifier.padding`，让滚动到边界时不被裁。
   - 横向内边距从 20dp 视情况减到 16dp（长段落阅读更舒展）。
   - Scaffold 显式设 `contentWindowInsets = WindowInsets(0)`，让正文占用到屏幕物理边缘；
     系统状态栏 / 导航栏的可见性由 chrome 自身用 `statusBarsPadding()` / `navigationBarsPadding()` 解决。

### 实现要点（仅规划，不做代码）
- 引入 `chromeVisible: MutableState<Boolean>`，初值 `false`。
- 段落容器外层加 `Modifier.pointerInput(Unit) { detectTapGestures(onTap = { toggleChrome() }) }`，
  但**要避开段落文本区域**：
  - 方案 A：在 LazyColumn 之外再套一层 Box，把点击监听挂到 Box 空白处（最简单但段落间空白少时难命中）。
  - 方案 B：在每条段落的 `Spacer` 间隔处挂点击（小段衔接处空白足够）。
  - 方案 C（推荐）：监听 LazyColumn 的 `clickable` 但设置 `indication = null` 且仅在 **段落外** 的容器 Box 上挂。
- `Scaffold` 的 topBar/bottomBar 改成 `if (chromeVisible.value) { ... }` 包裹并用 `AnimatedVisibility` 过渡。
- 滚动时短暂显示 chrome：用 `rememberLazyListState().isScrollInProgress` 驱动 1.5s 自动重新隐藏。

### 涉及文件
- `app/src/main/java/com/eareyereading/ui/screens/reader/ReaderScreen.kt:113-270`（Scaffold + 段落实体容器）
- `app/src/main/java/com/eareyereading/ui/screens/reader/ReaderScreen.kt:254-262`（bottomBar = ReadingBottomBar）
- `app/src/main/java/com/eareyereading/ui/screens/reader/ReaderScreen.kt` 全文：所有 `LazyColumn`（635、921、984 行）—— 它们各自有自己的 chrome 范围，需要分别评估
- 阅读模式（`NormalReadingView` / `RsvpReadingView` / `ClozeReadingView` 等）多份独立实现，
  沉浸态改动**必须全量统一**，避免某模式下 chrome 仍常驻

### 关联设计决策
- 与第 3 节"移除外观模块"绑定：沉浸态背景下 chrome 颜色需跟 ReadingTheme，
  移除 darkMode 后 MaterialTheme 与 ReadingTheme 联动，chrome 配色才不会再割裂。
- 与第 1 节"解压文案"间接相关：TtsInstallDialog 触发期间 chrome 应保持显示。

---

## 后续动作（待用户确认后执行）

- [ ] 修 Bug 1：解压进度预扫/解压循环口径一致（抽 `shouldCountTarEntry`）
- [ ] 修 Bug 4：解压末态分子分母对齐
- [ ] 修 Bug"设置页底部 SettingRow 硬编码文案"：跟 `embeddedDownloadStage` 联动，区分 Downloading/Extracting/Initializing
- [ ] 移除 SettingsScreen.kt:965-990 整段"外观"卡片
- [ ] SettingsScreen.kt:422-425 `setDarkMode` 兼容层决策（保留 / 删）
- [ ] MainActivity / Theme.kt 的 MaterialTheme 切换逻辑改为读 ReadingTheme
- [ ] 阅读页沉浸态：chrome 显隐 + 轻击空白切换（ReaderScreen.kt:113-270）
- [ ] 阅读页 chrome 颜色：跟 ReadingTheme 联动（依赖外观模块移除）

---

## 附：grep 命中一览

```
EmbeddedTtsEngine.kt:851-868   countTarEntries
EmbeddedTtsEngine.kt:947       _downloadProgress = Extracting(0, 0, null)
EmbeddedTtsEngine.kt:952       val preCountedEntries = countTarEntries(tarballFile)
EmbeddedTtsEngine.kt:967-970   动态分母兜底
EmbeddedTtsEngine.kt:1007-1012 解压循环过滤
EmbeddedTtsEngine.kt:1045-1052 末态 push

SettingsScreen.kt:807-831     阅读主题（ReadingTheme FilterChip）
SettingsScreen.kt:864-878     卡片内部进度条 + stage 文案（正确）
SettingsScreen.kt:905-912     卡片底部 SettingRow（硬编码，待修）
SettingsScreen.kt:965-990     "外观"段（深色模式 + Collins 高亮，待移除）
SettingsScreen.kt:422-425     setDarkMode 函数
SettingsScreen.kt:442-444     setCollinsHighlight 函数

ReaderScreen.kt:102-111       阅读页背景渲染（DarkBg/SepiaBg 硬编码）
ReaderScreen.kt:113-270       Scaffold + 段落容器（chrome 沉浸态改造范围）
ReaderScreen.kt:254-262       bottomBar = ReadingBottomBar（沉浸态需条件挂载）
ReaderScreen.kt:269           横向 20dp padding
ReaderScreen.kt:494-495       TtsInstallDialog fallback 文案
ReaderScreen.kt:635,921,984   各模式 LazyColumn（chrome 需统一）
ReaderScreen.kt:1930-1936     阅读页内 Collins 词频色彩 Switch

ReaderViewModel.kt:126        showWordLevelColors 默认 false
ReaderViewModel.kt:1768-1770  toggleWordLevelColors（不写 DataStore）
```

---

# 第二轮：系统 Bug 审查（2026-08-31）

> 范围：4 路并行 sub-agent + 主 agent 直接审查，覆盖 tts/reader/IO/settings 4 个域。
> 仅记录，未做代码修改。
> 标注 **[v]** 的为 sub-agent / 主 agent **重复独立发现**的 bug（高置信度）。

---

## 6. EmbeddedTtsEngine — TTS 引擎真实 bug

### 6.1 [P0] AudioTrack buffer 小于设备最小值 → 长句静默丢失
- **文件**：`tts/EmbeddedTtsEngine.kt:1705-1722, 1731-1734`
- **现象**：长句（>2 秒朗读）的最后一个 chunk `track.write()` 返回 `ERROR_BAD_VALUE` (-22)，整段不发声，**doSpeakLocked 继续推进到下一句**。用户听到"句间停顿 1-2 秒 → 继续读下一句"，但没有任何报错提示。
- **根因**：`val bufferSize = pcm16.size * 2` 正好等于数据本身，没有富余；`MODE_STATIC` 模式要求 buffer ≥ `dataSize + 16KB 头部开销`。
- **修法**：
  ```kotlin
  val minBuffer = AudioTrack.getMinBufferSize(sampleRate, AudioFormat.CHANNEL_OUT_MONO, AudioFormat.ENCODING_PCM_16BIT)
  if (minBuffer <= 0) return
  val bufferSize = maxOf(pcm16.size * 2, minBuffer)
  ```

### 6.2 [P0] `$.50` / `$0.99` 读成 "dollars point fifty"
- **文件**：`tts/EmbeddedTtsEngine.kt:370-373, 380-387, 403`
- **现象**：`"It costs $.50"` → 朗读 *"It costs dollars point fifty"*；`"$10.5"` → *"ten dollars point five"*。整体含义被歪曲。
- **根因**：三个 regex 覆盖不严密：
  - L370 `\\$(\\d+)` 要求 `$` 后是完整整数 → `$.50` / `$0.99` 不匹配
  - L380 `(?<!\\d)(\\d+)(?!\\d|:)` 抓 `50`、`99` 走 `fifty` / `ninety-nine`
  - L403 `s.replace("$", " dollars ")` 把 `$` 换掉
  最终 `$.50` → ` dollars .fifty`。
- **修法**：L370 正则改为 `\\$(\\d+(?:\\.\\d+)?)`，替换 lambda 区分整数/小数（"X dollars Y cents" / "X dollars point Y"）。

### 6.3 [P0] fullStream 取消探测在最后 256KB 几乎不触发
- **文件**：`tts/EmbeddedTtsEngine.kt:1381-1384`
- **现象**：用户在 99% 进度离开页面，但下载继续跑完最后 1-2MB，最坏 ~2-10 秒才响应取消。
- **根因**：
  ```kotlin
  if (totalRead % 262144L < read) {
      ensureActive()
  }
  ```
  当 `read` 越来越小（最后一包 < 1KB），`totalRead % 262144L < read` 概率仅 ~0.4%，**最后 256KB 段大概率永远不调 `ensureActive()`**。`appendStream` 用 `sinceCheck` 累加器是 OK 的。
- **修法**：改用 `sinceCheck` 累加器（与 `appendStream` 一致）。

### 6.4 [P0] `conn?.inputStream != null` 探测副作用消费首字节 → 200/206 响应被截断
- **文件**：`tts/EmbeddedTtsEngine.kt:1225-1232`
- **现象**：间歇性 "connection reset by peer" 时 socket 未及时归还池；高并发（设置页 + 阅读页同时触发下载）下 `java.net.SocketException: No buffer space available`。
- **根因**：`conn?.inputStream != null` 的探测有副作用 —— 某些 server 在 `getInputStream()` 时立即开始读 socket buffer，**真正的 200/206 响应被提前消费了一小段**。后续 `streamResponse` 再 `conn.inputStream.use { ... }` 时剩余字节比预期少，**文件残缺**。解压 bzip2 报 "invalid header"，`cleanExtractionPartials` 清理掉所有产物，**用户重试仍失败**。
- **修法**：用 `responseCode in [200, 299] || 206` 作为判定（已在 `code` 变量），不再探测 `inputStream`。

### 6.5 [P1] `MODE_STATIC` 立即 release 导致句尾 100-200ms 截断
- **文件**：`tts/EmbeddedTtsEngine.kt:1687-1698, 1784-1790`
- **现象**：每句话尾音在元音中间被打断，连起来像"结巴"。
- **根因**：`playPcm` 用 `while (elapsed < durationMs) { delay(50); ... }` 估算时长；新 chunk 立即 `release()` 旧 track，但 `MODE_STATIC` 模式下 `release()` 会**强制终止**正在被硬件 buffer queue 消费的音频 → 丢尾部 sample。
- **修法**：用 `setNotificationMarkerPosition + OnPlaybackPositionUpdateListener` 取代 `while` 循环。

### 6.6 [P1] cancel 分支 + finally 双重 `disconnect()` 产生 logcat 噪声
- **文件**：`tts/EmbeddedTtsEngine.kt:1219-1232`
- **现象**：Android 13+ 偶发 `IllegalStateException: disconnect() called on already disconnected connection` 冒到 logcat 顶层。
- **根因**：L1220 cancel 分支已 `conn?.disconnect()`；L1231 finally 又调一次。
- **修法**：删 L1220，仅保留 finally 路径。

### 6.7 [P1] 状态写入耦合在 `speakMutex` 锁内
- **文件**：`tts/EmbeddedTtsEngine.kt:1404-1417`
- **现象**：状态/锁耦合反模式 —— `_state.value = EngineState.READY(modelInfo.id)` 在 `speakMutex` 锁内写，外层 collect 立即看到 READY，但 `speak()` 还没真正播放，**界面/状态与实际播放脱节**。
- **修法**：把 `_state.value = ...` 移到 `speakMutex.withLock` 块**外**。

### 6.8 [P1] 句子链无 buffer 间隔，AudioTrack 切换产生"啵"声
- **文件**：`util/TtsHelper.kt:167-183` + `EmbeddedTtsEngine.kt:1687-1726`
- **现象**：长段落（>10 句）后几句开始可听见"啵"声；logcat 偶现 `AudioTrack: AUDIO_OUTPUT_FLAG_FAST denied by client`。
- **根因**：`speak()` 完成后无 delay，下一句立刻 `playPcm` 抢占：旧 track 的 native buffer 还在被硬件消费 → 抢占冲突。
- **修法**：TtsHelper `speak()` 后加 `delay(20)`；或让 `speak()` 内部等 AudioTrack marker（见 6.5 修法）。

### 6.9 [P2] `preprocessForTts` 缩写无上下文，`CT scan` 误读 "Central Time scan"
- **文件**：`tts/EmbeddedTtsEngine.kt:454-465`
- **现象**：`"the CT scan"` → *"the Central Time scan"*；`"the MT route"` → *"the Mountain Time route"*。
- **根因**：`\\bCT\\b` / `\\bPT\\b` / `\\bMT\\b` / `\\bET\\b` 无上下文约束。
- **修法**：高频歧义缩写（CT/PT/MT/ET）需要 POS tagger 配合或显式词典（`PosTagger` 已 import 但未用）。

### 6.10 [P2] CancellationException 路径不删 tarball 残片
- **文件**：`tts/EmbeddedTtsEngine.kt:820-826`
- **现象**：下载 66MB 模型到一半关页面 → 残留几 MB 到几十 MB 不完整 tarball 永远占盘。
- **根因**：CancellationException 分支只 `cancelDownloadNotification` + 重置状态流，**没调 `cleanExtractionPartials` 清 tarball**。
- **修法**：L820-826 增加 `tarballFile.delete()` 和 `tarballComplete.delete()` 清理（调 `cleanExtractionPartials`）。

---

## 7. ReaderViewModel / ReaderScreen — 阅读页真实 bug

### 7.1 [P0] [v] 换书后旧书签 / 高亮短暂残留在新书 UI（"幽灵书签"）
- **文件**：`ui/screens/reader/ReaderViewModel.kt:673-695`
- **现象**：用户从 A 书切到 B 书的瞬间，B 书的书签图标和高亮还显示着 A 书的内容。
- **根因**：`_uiState.update {}` 在 `loadBook` 内只重置了部分字段，但**没有**清空 `bookmarkedParagraphs` 和 `highlights`。`bookmarksJob` / `highlightsJob` 在 745、761 行才被 re-launch —— 第一次 Flow 发射前 UI 仍持有旧 Set/Map。
- **修法**：在 673-695 的 update 块内补：
  ```kotlin
  bookmarkedParagraphs = emptySet(),
  highlights = emptyMap(),
  ```

### 7.2 [P0] [v] 换书不取消 `selectWordJob` / `sentenceTranslateJob` / `addToVocabulary` → 旧书生词记到新书 bookId
- **文件**：`ui/screens/reader/ReaderViewModel.kt:618-626, 1430-1434`
- **现象**：A 书点词弹词卡，立刻按返回/重进 B 书，A 书的 `selectWord` 异步结果写进 B 书的 `selectedVocab`；`addToVocabulary` 在 launch 中读 `currentBookId`（已是 B 书 id），A 书生词被记到 B 书的 bookId/title。
- **根因**：`loadBook` 取消列表（行 619-626）漏掉 `selectWordJob` / `sentenceTranslateJob`，且 `addToVocabulary` 是裸 `viewModelScope.launch`（未追踪）。
- **修法**：
  1. 626 行后加 `selectWordJob?.cancel(); sentenceTranslateJob?.cancel()`
  2. `addToVocabulary` 改成可追踪的 job，加 `myBookId` 快照对照

### 7.3 [P0] [v] `BackTranslationView.revealed` 用 `rememberSaveable` 串到新书
- **文件**：`ui/screens/reader/ReaderScreen.kt:1463`
- **现象**：A 书的回译模式点"查看原文"（revealed=true）→ 旋转屏幕 / 进程被杀恢复 / 重进任意其他书，仍显示"已揭示"态。
- **根因**：`var revealed by rememberSaveable { mutableStateOf(false) }` 没加 bookId key。
- **修法**：
  ```kotlin
  var revealed by rememberSaveable(bookId) { mutableStateOf(false) }
  ```

### 7.4 [P0] 朗读中段落（NORMAL `isCurrent && isAutoReading` 分支）完全不可点击查词
- **文件**：`ui/screens/reader/ReaderScreen.kt:663-748`
- **现象**：朗读时（NORMAL 模式）整段点击无响应 —— 双击句子翻译、单点击词查询全部失效。**核心功能在朗读时不可用**。
- **根因**：该分支不挂 `pointerInput`、不包 `TappableParagraphText`。
- **修法**：把朗读中分支也包到 `TappableParagraphText` 里（用不带高亮/不带词色的纯字符串作为 `text`）。

### 7.5 [P1] 挖空 FlowRow `Text.clickable` 可点区仅字符字形
- **文件**：`ui/screens/reader/ReaderScreen.kt:1063-1071, 1051-1082`
- **现象**：挖空模式点非隐藏词（已揭示的）极难点中；同时 hidden 状态没有"点空白处揭示"入口。
- **根因**：`Text` 直接 `.clickable{...}`，无 padding，可点区是字符字形外接矩形（很小）。
- **修法**：
  ```kotlin
  .padding(horizontal = 4.dp, vertical = 2.dp)
  .clickable { onWordClick(clozeWord.text) }
  ```
  hidden 文本加 `.clickable { onReveal() }`。

### 7.6 [P1] 朗读累计字数虚增（`nextParagraph`+300ms 防抖竞态）
- **文件**：`ui/screens/reader/ReaderViewModel.kt:1315-1348, 1629-1643`
- **现象**：300ms 内连续点 next（每次 idx+1），最终一次 `doSaveProgress` 看到 `lastRecordedParagraphIndex = 旧值`，`newParaIndex - lastRecordedParagraphIndex = N` 按"顺序阅读"累加 N 段 —— 中间段已被上一轮计入过，**字数虚增**。
- **根因**：`lastRecordedParagraphIndex` 在 300ms 防抖后才更新，prev/next/goTo 连续操作期间中间段被重复计字。
- **修法**：把 `lastRecordedParagraphIndex` 的更新放在 `_uiState.update` 时立刻执行（同步原子地），不要放到 300ms 后。

### 7.7 [P2] `TappableParagraphText` 段落滚出可见区再回来点击失效
- **文件**：`ui/screens/reader/ReaderScreen.kt:2197-2227`
- **现象**：LazyColumn 把段落 item 滚出可见区再滚回，**首次点击没有 layout**，点击失效直到下一次 layout。
- **根因**：`textLayoutResult` 用 `remember { mutableStateOf<TextLayoutResult?>(null) }`，不持久；item 离开 composition 时被丢弃，再回来是 null。
- **修法**：把 `textLayoutResult` 改用 `remember(paragraph)`（已部分尝试但 key 不全）；或用 `LaunchedEffect(paragraph)` 强制监听 layout 完成。

---

## 8. EmbeddedTtsEngine 之外的 TTS 辅助 bug

### 8.1 [P1] [v] `TtsHelper.speak()` 第 138-139 行 `cancel + launch` 竞态
- **文件**：`util/TtsHelper.kt:138-139`
- **现象**：`embeddedSpeakJob?.cancel()` 是非阻塞的，旧协程还在取消中，新协程 `scope.launch` 立即跑 —— 两个 `speak` 协程几乎同时跑。
- **根因**：旧协程 cancel 后到第一个挂起点才退出，但 `EmbeddedTtsEngine.speak` 内部用 `speakMutex.withLock` 串行化（不会出现 JNI 段错误），但 IO 阻塞（AudioTrack.write）时 cancel 不响应，**复合竞态**。
- **修法**：用 `Job.cancelAndJoin()` 等旧协程退出再 launch 新协程；或在 `EmbeddedTtsEngine.speak` 入口处加 `ensureActive()` 检测。

### 8.2 [P2] `CollinsClassifier.classify` 排除单字母词，`"I"` 误判 UNKNOWN
- **文件**：`util/CollinsClassifier.kt:2257`
- **现象**：`classify("I")` 应返回 CORE（Collins 1 星），但代码 `if (w.length < 2) return WordLevel.UNKNOWN` 返回 UNKNOWN。
- **根因**：`< 2` 守卫过严，但 `collinsOne` 包含 `"a", "i"`。
- **修法**：
  ```kotlin
  if (w.length < 2) {
      return if (w in collinsOneLower) WordLevel.CORE else WordLevel.UNKNOWN
  }
  ```
  或更新测试合同为"单字母 → UNKNOWN"（产品决策）。

---

## 9. 数据 / IO / 解析 — 安全 / 性能 bug

### 9.1 [P1] 词典下载响应体无字节上限（OOM / 写满磁盘）
- **文件**：`util/DictionaryManager.kt:369-430`
- **现象**：词典 URL 若被劫持或日后改成用户可配置，单次响应体可灌满 `dictDir`（`filesDir/dictionaries`），或 1GB+ 文本直接 OOM。
- **根因**：`downloadFile` / `downloadText` 均无 `MAX_DOWNLOAD_BYTES` 限制。
- **修法**：加 `MAX_FILE_BYTES = 500MB`、`MAX_TEXT_BYTES = 1MB`，超限抛 IOException 走 `finally` 删 `.tmp`。

### 9.2 [P1] ArticleParser 接受任意 URL scheme（ClassCastException 崩溃）
- **文件**：`util/ArticleParser.kt:44-73, 78-109`
- **现象**：
  1. `data:` / `file:` / `ftp:` / `mailto:` 的 URL，`URL(...).openConnection()` 返回非 `HttpURLConnection`，`as` 强转抛 `ClassCastException` —— `parseFromUrl` 没有 catch `Exception`，异常透传到 LibraryViewModel
  2. `extractLinksFromHtml`（L127）只过滤 `#` 与 `javascript:`，**不挡 `data:` / `mailto:` / `file:`**
- **根因**：
  - `parseFromUrl` L46-47 缺 scheme 白名单
  - `extractLinksFromHtml` L127 缺同样过滤
- **修法**：
  ```kotlin
  if (urlStr !in listOf("http://", "https://")) return null
  // 改为 catch (e: Exception) 兜底
  if (href.startsWith("#") || href.startsWith("javascript:") || href.startsWith("data:") 
      || href.startsWith("file:") || href.startsWith("mailto:") || href.startsWith("vbscript:")) continue
  ```

### 9.3 [P1] EpubParser `resolveEntry` 兜底分支 O(spine × totalEntries) 算法炸弹
- **文件**：`util/EpubParser.kt:94-101`
- **现象**：恶意 EPUB 在 OPF 里塞 20 000 个指向不存在 href 的 `<itemref>`，每条都走兜底分支；对 100 000 项 zip 总比较次数 = **20 亿次**。主线程 5-30 秒 ANR。
- **根因**：`resolveEntry` 第 100 行没限总条目数；`MAX_SPINE_ITEMS` 与 entries 总数解耦。
- **修法**：
  1. `parseEpub` 入口加 `if (zip.size() > MAX_ZIP_ENTRIES)` 拒绝（建议 5 000）
  2. 或 `resolveEntry` 兜底分支计数 > N 次（如 200）直接 `return null`
  3. 或循环外把 `zip.entries().toList()` 一次性建索引

### 9.4 [P1] EpubParser 没用条目级字节防护，依赖字符上限
- **文件**：`util/EpubParser.kt:51, 78`
- **现象**：5 000 000 entry 的 zip（每条 1 字节、1 字节数据 = 中央目录 ~200MB+）会 OOM。`MAX_SPINE_ITEMS = 20 000` 只防 spine 遍历，不防 `zip.entries()` 本身的内存占用。
- **根因**：与 EmbeddedTtsEngine 的 `MAX_EXTRACT_BYTES` 同量级的「总解压字节上限」缺失（注：grep 全文确认 EmbeddedTtsEngine 也**没有**这个常量，是用户记忆有误 —— 此处问题在 EpubParser 一贯缺少，不与 TTS 不一致）。
- **修法**：`parseEpub` 加 `if (file.length() > MAX_EPUB_BYTES)` 早返回（建议 200MB）；每个 entry 解压累计字节加 `totalBytes` 计数。

### 9.5 [P2] ArticleParser 无 Content-Type 校验，任意字节流当 HTML
- **文件**：`util/ArticleParser.kt:46-62`
- **现象**：用户喂 `https://attacker.example.com/payload.zip`，服务器返回 200 + `Content-Type: application/zip`。解码到 5 MB 字符就停，但 zip 头在正则面前是合法文本 → 抓出大量乱码"段落"入库，污染词频与生词本。
- **根因**：缺 content-type 白名单 + content-length 早判。
- **修法**：
  ```kotlin
  val ct = conn.contentType?.lowercase() ?: ""
  if (!ct.startsWith("text/html") && !ct.startsWith("application/xhtml")
      && !ct.startsWith("application/xml") && !ct.startsWith("text/plain")) {
      return null
  }
  if (conn.contentLengthLong > MAX_BODY_BYTES) return null
  ```

### 9.6 [P2] BookDao.searchBooks 用 `LIKE '%query%'`，全表扫描 + LIKE 通配符注入
- **文件**：`data/local/dao/BookDao.kt:36`
- **现象**：
  1. 已有索引对 `LIKE '%xxx%'` 完全失效（前导通配符禁用 B-tree）；1 000 本书的库每次按键都全表扫
  2. 用户输入 `_` 或 `%` 会被 SQLite 当通配符，无法按字面量搜
  3. 没有 size 限制，输入几 MB 字符串会让 SQLite 端 OOM
- **根因**：未做转义、未用 FTS5。
- **修法**：用 Room FTS5 取代 LIKE；或调用方 `query.escapeForLike()` 转义并用 `ESCAPE '\'`，并把搜索长度截断到 64 字符。

### 9.7 [P2] BookRepositoryImpl.deleteBook 文件清理在事务外，文件删除失败无回滚
- **文件**：`data/repository/BookRepositoryImpl.kt:132-159`
- **现象**：DB 事务提交成功但 `file.delete()` 失败，文件成孤儿。`books/` 目录在 `filesDir` 内但随书数增多占满空间。
- **根因**：删除文件与 DB 写入不在同一事务。
- **修法**：失败时把孤儿文件路径写一个 `orphan_files.txt`，下次启动 cleanup；或换 SAF 写入。

### 9.8 [P2] EpubParser.extractParagraphsFromHtml `filter { length > 10 }` 太激进
- **文件**：`util/EpubParser.kt:184`
- **现象**：章节标题"Chapter 1"（9 字符）、"INTRODUCTION"（12 字符，OK 但 borderline）、短的对话行、诗的标题、图注 全部被吞。
- **根因**：`< 10` 阈值过严。
- **修法**：改 `< 3` 或 `< 5`；或完全去掉这个过滤，让 UI 去重。

### 9.9 [P2] EpubParser.extractManifestItems 双正则 + 错位 manifest
- **文件**：`util/EpubParser.kt:136-154`
- **现象**：恶意 OPF 用 `<item href="a" id="b"/><item id="b" href="a"/>` 错位，最终映射可能错位。后续 `resolveEntry` 会把图片当 HTML 读 → 该章节被静默丢弃，**无报错**。
- **根因**：双正则合并逻辑不严谨。
- **修法**：用 `XmlPullParser` 解析 OPF；或把双 regex 结果统一进 `LinkedHashMap` 保留先到者。

### 9.10 [P2] ArticleParser.cleanText 段落按"每 4 句合并"硬切
- **文件**：`util/ArticleParser.kt:296-307`
- **现象**：句子按 `(?<=[.!?])\\s+(?=[A-Z])` 切，要求句子首字母大写 —— 对话 `"I said, hello."` 后跟小写 `she` 不切，整段成一句。
- **根因**：split 正则太严。
- **修法**：放宽首字母约束（`(?=[A-Z])|(?=[a-z])` 仅在 `I` 类时用）或者直接按 `\\.\\s+` 切再合并。

### 9.11 [P3] ArticleParser unescapeJson 死代码 + 缩进错位
- **文件**：`util/ArticleParser.kt:191-200`
- **现象**：`hex` 变量取自 `s.getOrNull(i+2)?.toString()`，永远为 "null" 4 字符或单字符。判断条件 `hex.isNotEmpty()` 永远为 true（除 `s.length <= i+2` 边界），**死代码**。
- **根因**：变量名错位，逻辑靠 `hex4.length == 4` 这条正确守卫兜住。
- **修法**：直接删 `hex` 变量与 `hex.isNotEmpty()` 条件。

---

## 10. 设置 / 通知 / 导航 — bug

### 10.1 [P0] [v] `EareyeReadingApp.onCreate` 主线程 `runBlocking` 读 DataStore → 启动卡顿
- **文件**：`EareyeReadingApp.kt:41-58`
- **现象**：冷启动时 DataStore Preferences 解码 + 文件读会 >500ms，触发 StrictMode `DiskReadViolation`，用户感知"打开 App 卡 1-2 秒白屏"。
- **根因**：`Application.onCreate` 内 `runBlocking(withTimeoutOrNull(2_000))` 阻塞主线程；最坏 2 秒。
- **修法**：
  ```kotlin
  private val appScope = CoroutineScope(SupervisorJob() + Dispatchers.IO)
  override fun onCreate() {
      super.onCreate()
      notificationHelper.createNotificationChannel()
      appScope.launch { syncReminderPrefsMirror() }
  }
  ```
  2 秒超时改为异步；提醒链重排依赖 WorkManager 持久化。

### 10.2 [P0] [v] BootReceiver 监听 `ACTION_TIME_SET` / `ACTION_TIMEZONE_CHANGED` 在 Android 5+ 不再发送
- **文件**：`receiver/BootReceiver.kt:16-32` + `AndroidManifest.xml:66-75`
- **现象**：用户跨时区旅行或手动改系统时间后，**复习提醒闹钟不再重新调度**。
- **根因**：Android 5+ 起 `ACTION_TIME_SET` 与 `ACTION_TIMEZONE_CHANGED` 不再发送给 manifest-declared receiver（仅动态注册的应用可收到）。
- **修法**：
  - 选项 A：BootReceiver 不再监听这两个 action，改在 `EareyeReadingApp.onCreate` 动态注册 `BroadcastReceiver`
  - 选项 B：`MainActivity.onResume` 检测 `System.currentTimeMillis() - lastScheduleTime > 12h` 主动重排

### 10.3 [P0] [v] ReviewViewModel.answerCard 索引越界 + 双重写入
- **文件**：`ui/screens/review/ReviewViewModel.kt:107-190`
- **现象**：评分记到下一张未展示的卡片；用户答完后顶部 Badge 数字跳动。
- **根因**：`state = _uiState.value` 拍快照后，`state.dueCards[state.currentIndex]` 直接索引访问（line 113），但 `dueCountTimestamp.value = System.currentTimeMillis()` 紧跟写库后执行 → flatMapLatest 重新发射。L164/172/173 混用快照（`state`）与最新值（`it`）。
- **修法**：line 164/172/173 全部使用同一份 state 快照；line 113 在写库前再判一次 `if (state.currentIndex >= state.dueCards.size) return`。

### 10.4 [P0] SettingsScreen Snackbar 重复消息丢失
- **文件**：`ui/screens/settings/SettingsScreen.kt:710-715`
- **现象**：连续触发两个错误信息（如导入失败 + 缓存清理失败），第二条消息在 `snackbarHostState.showSnackbar(msg1)` 还在挂起期间 emit 到 `uiState.snackbarMessage`，但 `LaunchedEffect(uiState.snackbarMessage)` 因为 String 值相等不重启 effect —— **同条消息丢失**。
- **根因**：`LaunchedEffect(uiState.snackbarMessage)` 仅在 `uiState.snackbarMessage` 变化时重启；`msg1 == msg2` 时被 drop。
- **修法**：用 `LaunchedEffect(uiState.snackbarMessage, uiState.snackbarMessage != null)` 双键；或 ViewModel 侧每次 emit 加 `eventId: Long` 计数器。

### 10.5 [P0] [v] ReviewViewModel SM-2 公式与原始论文不符 —— `q<3` 不衰减 EF
- **文件**：`ui/screens/review/ReviewViewModel.kt:117-122`
- **现象**：用户标记"忘了（q=1）"或"困难（q=2）"后，easeFactor 不变；但 SM-2 规定 `q<3` 时 `EF' = max(1.3, EF - 0.2)`。连错几题后 EF 仍然偏高，下次答对时 `interval * EF` 计算的复习间隔被错误放大，违背遗忘曲线。
- **根因**：line 118-122 `if (q < 3) record.easeFactor else ...` 与 SM-2 原始论文不符。
- **修法**：
  ```kotlin
  val newEF = max(1.3f, record.easeFactor + (0.1f - (5 - q) * (0.08f + (5 - q) * 0.02f)))
  ```
  删 if 分支。

### 10.6 [P0] DictionaryManagerScreen `items(count, key)` 索引变化 → Compose 崩溃/错乱
- **文件**：`ui/screens/dictionary/DictionaryManagerScreen.kt:270-278`
- **现象**：词典管理页在下载完成后 list 重建时闪退 / state 错乱。
- **根因**：`items(uiState.statuses.size, key = { uiState.statuses[it].info.id })` 当 `uiState.statuses[it]` 因为新发射变了新 DictionaryInfo 时 key 改变 → Compose 认为该位置 item 类型变了。
- **修法**：
  ```kotlin
  items(items = uiState.statuses, key = { it.info.id }) { status -> ... }
  ```

### 10.7 [P1] HomeViewModel `loadDataJob?.cancel()` 旧 job 的 combine flow 不停止 collect
- **文件**：`ui/screens/home/HomeViewModel.kt:76-105`
- **现象**：已 cancel 的协程持有的数据库连接不释放（Flow 发射源仍 active）。
- **根因**：`loadData` 内 `combine(...).collect()` 在 launch 协程中，但 `combine` 不会响应 `viewModelScope` cancel 立即停止。
- **修法**：`loadDataJob?.cancelAndJoin()` 等待旧 job 完全退出；或在 `loadData` 内把 `combine` 放到 `loadDataJob = viewModelScope.launch { combine(...).collect() }` 中，让 cancel 传播。

### 10.8 [P1] LibraryViewModel.deleteBook 缺 try-catch
- **文件**：`ui/screens/library/LibraryViewModel.kt:345-349`
- **现象**：DB 异常（如外键约束）会让 ViewModel 崩溃到默认 handler。
- **根因**：裸 `viewModelScope.launch` 无 try-catch。
- **修法**：
  ```kotlin
  fun deleteBook(bookId: Long) {
      viewModelScope.launch {
          try {
              bookRepository.deleteBook(bookId)
          } catch (e: Exception) {
              android.util.Log.e("LibraryVM", "deleteBook failed", e)
              _uiState.update { it.copy(loadingMessage = "删除失败：${e.message}") }
          }
      }
  }
  ```

### 10.9 [P1] ReviewScreen 加载中显示白屏
- **文件**：`ui/screens/review/ReviewScreen.kt:94-110`
- **现象**：DB 查询 1-3 秒内看到**白屏**。
- **根因**：line 97 `if (currentCard != null)` 既不渲染 spinner 也不渲染"加载中"提示。
- **修法**：
  ```kotlin
  else if (uiState.dueCards.isEmpty() && uiState.errorMessage == null && !uiState.isSessionComplete) {
      CircularProgressIndicator()
  }
  ```

### 10.10 [P1] SettingsViewModel.deleteEmbeddedTts 删文件时 TTS 仍在跑 → SIGSEGV
- **文件**：`ui/screens/settings/SettingsScreen.kt:392-406`
- **现象**：用户点"删除语音模型"——`embeddedTts.release()` 紧接 `deleteModel()` 删除磁盘文件，若 `speak()` 协程仍在跑、即将读取文件 → SIGSEGV。
- **根因**：`release()` 的实现是 `tts?.release()`，native 释放与磁盘删除之间的时序不一定有保证。
- **修法**：line 400 改为 `delay(50)` 或加 suspend 等待 TtsHelper.onEmbeddedReleased 回调完成；或 `deleteModel()` 也走 suspend 路径。

### 10.11 [P2] HomeScreen 顶部日期跨午夜不更新
- **文件**：`ui/screens/home/HomeScreen.kt:41`
- **现象**：用户打开 App 跨过午夜时日期仍显示昨天。
- **根因**：`SimpleDateFormat("MM月dd日 E", Locale.CHINA).format(Date())` 在 `remember` 中只算一次。
- **修法**：用 `LaunchedEffect` + `delay(60_000)` 周期性 tick；或监听 `Lifecycle.Event.ON_RESUME` 刷新。

### 10.12 [P2] Snackbar 同字符串去重问题（同 10.4，影响多个屏）
- **文件**：`ui/screens/library/LibraryScreen.kt:46-52` 等多处
- **现象**：第二次同样文案的消息被 `LaunchedEffect` 静默吞掉。
- **修法**：同 10.4 —— ViewModel 侧 emit 加 `eventId`。

### 10.13 [P3] SettingsScreen import 文件选择器 MIME 数组含 `*/*` 覆盖前面限定
- **文件**：`ui/screens/settings/SettingsScreen.kt:1013`
- **现象**：`arrayOf("application/json", "text/plain", "*/*")` 含 `*/*` —— 前两个 MIME 限定完全被覆盖，用户看到所有类型文件。
- **修法**：去掉 `*/*`，或加文档说明为何需要 fallback。

---

## 11. 生命周期 / 性能 / 主线程 IO bug

### 11.1 [P1] [v] ReaderViewModel.cleanup() 是阻塞同步 IO，违反 ViewModel.onCleared 契约
- **文件**：`ui/screens/reader/ReaderViewModel.kt:1710-1744`
- **现象**：用户感觉"退出页面卡一下"。
- **根因**：`onCleared` 调 `cleanup()`，内部 `runBlocking(Dispatchers.IO) { ... doSaveProgress() ... }` —— Room 在 IO 上跑没问题，但**外层把当前线程（Main）切到 IO**，**整个 cleanup 阻塞在 Main 线程**，等所有 IO 完成才返回。Room 在 IO 上卡 500ms → Main 线程被卡 500ms。
- **修法**：
  - 在 onCleared 之前**主动保存**（如 DisposableEffect onDispose 调 `saveProgress()`）
  - 或用 `goAsync()` 的 WorkManager 兜底
  - 或在 onCleared 之前先 fire-and-forget 协程到 Application 全局 scope

---

## 12. 已审但**未发现**真 bug 的点（确认安全）

- `EpubParser` 缺 zip slip —— **正确**。从不把 `ZipEntry.name` 写盘，无路径穿越面
- `BookRepositoryImpl.deleteBook` canonical path 校验 —— **正确**（L149-153）
- `DictionaryManager.safeDictFile` canonical path 校验 —— **正确**（L192-201）
- DataStore 并发写 —— Android `DataStore<Preferences>.edit` 内部 `Mutex` 串行化，**安全**
- `ReviewRecordDao` / `BookmarkDao` 用 `OnConflictStrategy.IGNORE` + 唯一索引防 race —— **正确**
- `RssParser` 的 `MAX_BODY = 10_000_000` 字节上限 —— **正确**
- `EmbeddedTtsEngine` 没有 `MAX_EXTRACT_BYTES` 常量（用户提示有误）—— grep 全文无匹配

---

## 第二轮后续动作（待用户确认后执行）

按修复 ROI 排序：

1. **P0 必修**（数据损坏 / 启动卡顿 / 核心功能失效）：
   - 6.1 AudioTrack buffer（P0）
   - 6.4 `conn?.inputStream` 探测副作用（P0）
   - 7.1 / 7.2 / 7.3 / 7.4 阅读页换书 + 朗读中点击（P0 × 4）
   - 10.1 / 10.2 Application 启动 + BootReceiver（P0 × 2）
   - 10.3 / 10.4 / 10.5 / 10.6 Review / Settings 状态机（P0 × 4）
2. **P1 优先**（用户能感知但不致命）：
   - 6.5 / 6.6 / 6.7 / 6.8 TTS 音频细节
   - 7.5 / 7.6 阅读页交互
   - 8.1 / 9.1 / 9.2 / 9.3 / 9.4 IO 安全
   - 10.7 / 10.8 / 10.9 / 10.10 / 11.1 生命周期
3. **P2 / P3 可下迭代**：UI 体验 / 死代码清理
