# eareyereading 问题清单（按状态重组版）

> 仓库：`KevinSnoopy/mavis-workspace`，分支 `eareyereading`
> 整理日期：2026-08-31　｜　重组日期：2026-09-01（按状态重组，原编号保留为稳定标识）
> 范围：TTS 引擎 / 阅读页 / 设置 / 通知 / 文章解析 / EPUB 导入 / 翻译 / 复习 / 生词本 / 词典
> 格式：每条 = 现象 / 根因 / 修法 / 涉及文件
> 标注 **[v]** = 多源独立发现的高置信度 bug
> 优先级：P0 数据丢失/崩溃/核心功能失效 / P1 主流程错误/严重 UX / P2 边界/性能 / P3 死代码/异味
> **状态来源**：第六节修复记录历史（权威）＋ 代码复核（首批/第二批未逐条回标项按代码实态确认）
> **编号策略**：原编号（1.1 / 2.3 / 9.5 …）不再重排，作为稳定标识继续使用（代码注释 / 文件索引均引用之）

---

## 一、状态总览

### 1.1 总体统计

| 类别 | 数量 |
|------|------|
| 问题条目总数 | 97 |
| └ 已修复 | 71 |
| └ 待处理 | 26 |
| 已确认安全（非 bug） | 7 |

> 变化：第五批（2026-09-01）新增修复 13 项（见第六节 6.3），含 8.4/8.6/8.8/10.1/10.7/11.5/11.7/11.12/11.15/11.17/11.19/12.6/3.7。

### 1.2 按模块统计

| 模块 | 总数 | 已修复 | 待处理 | 备注 |
|------|-----:|------:|------:|------|
| 1 设置页 TTS 下载 | 3 | 2 | 1 | 剩 1.3 性能优化 |
| 2 TTS 引擎 | 12 | 10 | 2 | 2.7 设计取舍 / 2.10 POS 联动 |
| 3 阅读页 | 10 | 6 | 4 | 3.7 已修（第五批）；3.6/3.8-3.10 待 |
| 4 主题/外观 | 3 | 1 | 2 | 4.1/4.3 产品决策 |
| 5 通知 | 5 | 0 | 5 | 待统一 NotificationService |
| 6 通知时机/重启 | 2 | 2 | 0 | 6.2 已修（第五批，动态注册） |
| 7 文章解析/RSS | 5 | 5 | 0 | |
| 8 翻译 | 10 | 8 | 2 | 8.4/8.8 已修（第五批）；8.1/8.5 待 |
| 9 EPUB 导入 | 10 | 8 | 2 | 9.2/9.7 部分完成；9.9/9.10 待修 |
| 10 IO/性能/安全 | 11 | 11 | 0 | 10.1/10.7 已修（第五批） |
| 11 复习页 | 20 | 14 | 6 | 11.5/11.7/11.12/11.15/11.17/11.19 已修 |
| 12 生词本/词频/词典 | 6 | 4 | 2 | 12.6 已修（第五批）；12.3/12.5 待修 |
| **合计** | **97** | **71** | **26** | |

> 说明：12.4 为 8.7 的重复指向，随 8.7 一并计入已修复。

---

## 二、待处理问题（未修复）

> 按优先级排序，保留原编号与完整详情；已修复条目见第三节。
> 各条目标题保留模块前缀以便溯源。

### 2.1 P0（数据丢失 / 崩溃 / 核心功能失效）

#### 3.8 [P0] 阅读页 屏占比不足 + 操作栏常驻挤压阅读空间（产品决策）
- **文件**：`ui/screens/reader/ReaderScreen.kt:113-270`
- **现象**：TopAppBar 64dp + 状态栏 24-30dp = 总 88-94dp；BottomBar 56-72dp + 导航栏 28dp = 总 84-100dp。6.5 寸屏吃 18% 屏占比。
- **期望**：
  1. 默认进入沉浸态（TopAppBar + BottomBar 隐藏）
  2. 轻击段落**之间**的空白（不是段落文字）切换 chrome 显隐
  3. `AnimatedVisibility` 200ms 过渡，不要瞬切
  4. 滚动 LazyColumn 时短暂显示 chrome（scroll-driven reveal）
  5. 弹窗/选词/TTS 播放时必须显示 chrome
  6. `Scaffold.contentWindowInsets = WindowInsets(0)` 让正文占满物理边缘
- **修法**：引入 `chromeVisible: MutableState<Boolean>` 初值 `false`；段落容器外层加 `pointerInput { detectTapGestures }` 但要避开段落文字区域（推荐方案 C）。

#### 3.9 [P0] 全局 所有页面顶部留白过多，状态栏高度未被正确纳入（产品决策）
- **文件**：`MainActivity.kt:25-39` / `ui/Navigation.kt:79-102` / 6 个主页面 Scaffold
- **现象**：Home / Library / Vocabulary / Review / Settings / DictionaryManager 全部用 M3 TopAppBar，**总高度 = 64dp + 状态栏 24-30dp ≈ 88-94dp**，6.5 寸屏吃 10% 屏占比。
- **根因**：
  - `MainActivity.kt:28` 调了 `enableEdgeToEdge()`
  - 根 Scaffold 没设 `contentWindowInsets = WindowInsets(0)`
  - 各页面 TopAppBar 用默认 `windowInsets = TopAppBarDefaults.windowInsets`（含 statusBars）
- **修法（三选一）**：
  1. 根 Scaffold `contentWindowInsets = WindowInsets(0)` + 各页 TopAppBar `windowInsets = WindowInsets.statusBars` 显式声明
  2. 自定义 TopAppBar 包装，强制 `height = 64dp + statusBars.getTop()`
  3. 用 `SmallTopAppBar` 替代 + 外层 `.statusBarsPadding()`
- **涉及文件**：6 个主页面 Scaffold + 根 Navigation.kt

#### 3.10 [P0] 阅读页 清理 `onCleared()` 是阻塞同步 IO，违反 ViewModel 契约
- **文件**：`ui/screens/reader/ReaderViewModel.kt:1710-1744`
- **现象**：用户感觉"退出页面卡一下"。
- **根因**：`runBlocking(Dispatchers.IO) { ... doSaveProgress() ... }` 把 Main 线程切到 IO，整个 cleanup 阻塞在 Main。
- **修法**：在 onCleared 之前主动保存（DisposableEffect onDispose 调 `saveProgress()`）；或用 `goAsync()` WorkManager 兜底。
- **现状**：已加 onDispose 异步保存路径（`cleanup(synchronous=false)`），onCleared 仍保留同步兜底。

#### 5.1 [P0] 通知 权限仅在设置页拨开关时申请，首启/其他场景无入口
- **文件**：`ui/screens/settings/SettingsScreen.kt:663-671, 949-953`
- **现象**：所有页面中**只有** `SettingsScreen.kt:949-953` 会调 `notificationPermissionLauncher.launch(POST_NOTIFICATIONS)`。`MainActivity` / `EareyeReadingApp` / 阅读页 / 复习页 / 主页全没有。用户首启直接进首页，权限从未被申请，到点通知静默被系统吞。
- **修法**：
  - **首启 onboarding 引导页**（F9 已有 3 页扩展到 4 页）："开启通知不错过每日复习"，大按钮"开启通知"调 `notificationPermissionLauncher.launch(POST_NOTIFICATIONS)`
  - **复习页完成** / **阅读完成** / **主页有 due card**时也允许申请
  - **被拒一次后**（"don't ask again"被勾选）：弹 Snackbar + "去设置"按钮 `Intent(Settings.ACTION_APP_NOTIFICATION_SETTINGS)`
  - 同步把 `notifications_enabled` DataStore 写为 `false`
  - 统一封装 `NotificationPermissionRequester`（Composable + 工具函数）
- **涉及文件**：
  - `ui/screens/settings/SettingsScreen.kt:663-671, 949-953`
  - `MainActivity.kt:25-39`（缺引导页申请）
  - `EareyeReadingApp.kt:22-27`
  - `ui/screens/review/ReviewScreen.kt` / `ui/screens/reader/ReaderScreen.kt` / `ui/screens/home/HomeScreen.kt`（加申请入口）
  - 新建 `util/NotificationPermissionRequester.kt`

#### 5.2 [P0] 通知 通知服务（NotificationService）完全没实现
- **文件**：`AndroidManifest.xml`（无 `<service>`）/ 通知散落 3 处
- **现象**：
  - AndroidManifest 没有任何 `<service>` 声明
  - 没有任何类继承 `Service` / `JobIntentService` / `Worker`
  - 通知发送逻辑散落 3 处：`NotificationReceiver.kt:42-59` / `EmbeddedTtsEngine.kt:1867+ showDownloadNotification 系列` / 没第三处
  - 没有通知分组 / 通知历史 / 通知点击事件埋点 / 通知偏好
- **修法**：
  - 新增 `NotificationService`（Hilt @Singleton）：
    ```kotlin
    @Singleton
    class NotificationService {
        fun showReviewReminder(dueCount: Int)
        fun showTtsDownloadProgress(percent: Float, message: String?)
        fun showTtsDownloadComplete(message: String)
        fun showTtsDownloadFailed(reason: String)
        fun cancelTtsDownloadNotification()
        fun cancelAllNotifications()
    }
    ```
  - **Channel 分类**：
    - `eareye_review_reminder` (IMPORTANCE_DEFAULT) 复习提醒
    - `eareye_tts_download` (IMPORTANCE_LOW) TTS 下载进度
    - `eareye_tts_complete` (IMPORTANCE_DEFAULT) TTS 下载完成
    - `eareye_error` (IMPORTANCE_HIGH) 错误告警
  - 所有通知通过 `NotificationService` 发送
- **涉及文件**：
  - 新建 `util/NotificationService.kt`
  - `receiver/NotificationReceiver.kt:42-59`
  - `tts/EmbeddedTtsEngine.kt:1867+`
  - `util/NotificationHelper.kt:41-58`
  - `AndroidManifest.xml:39-77`（如用 ForegroundService）
  - 新建 `di/NotificationModule.kt`

#### 8.1 [P0] 翻译 翻译功能硬编码 EN→ZH，中文/法文/日文书点词翻译全废
- **文件**：`util/TranslationHelper.kt:86-89, 129-159, 162-166`
- **现象**：用户从中文 RSS 源添加文章 / 导入中文 EPUB（`BookEntity.language` 默认 "en"）→ ML Kit 收到中文短串 → 翻译结果为空 / 重复 / 乱码。
- **根因**：`TranslatorOptions.Builder().setSourceLanguage(ENGLISH).setTargetLanguage(CHINESE)` 写死 EN→ZH。`BookEntity.kt:27` `language: String = "en"` 默认。`LibraryViewModel.kt:244, 317, 511` 三处 `Book(...)` 构造不传 language。`ArticleResult` 没有 `language` 字段。
- **修法**：
  1. `TranslationHelper` 改造成 `translate(sourceLang, targetLang, text)`，按语言对缓存多份 `Translator`（`ConcurrentHashMap<Pair<String,String>, Translator>`）
  2. `ReaderViewModel.selectWord` 读 `_uiState.value.book?.language ?: "en"` 决定 sourceLanguage
  3. `Book` 模型新增 `targetLanguage`（默认 zh）
  4. `LibraryViewModel` 三处 `Book(...)` 按源域名 / 内容启发式（首字符是否 CJK）推断 language
  5. `ArticleResult` 加 `language`，`ArticleParser` 在 `extractContent` 后判 language
- **涉及文件**：
  - `util/TranslationHelper.kt:86-89, 129-159, 162-166`
  - `ui/screens/reader/ReaderViewModel.kt:1390-1427, 1511-1576, 1787-1809`
  - `data/local/entity/BookEntity.kt:27`
  - `domain/model/Models.kt:14`
  - `ui/screens/library/LibraryViewModel.kt:244, 317, 511`
  - `util/ArticleParser.kt:313-316`

### 2.2 P1（主流程错误 / 严重 UX）

#### 1.3 [P1] 设置页 TTS 解压耗时过长（5 个可优化点）
- **文件**：`tts/EmbeddedTtsEngine.kt:851-868, 947-1056, 262-266`
- **现象**：Piper 66MB bz2 归档解压总耗时中端机 4-6 秒、低端机 8-15 秒，用户看着"统计文件数…"不动以为卡死。
- **根因**：
  1. `countTarEntries` 完整跑一遍 bzip2 + tar（不写盘）—— **bzip2 被解码两次**
  2. `BZip2CompressorInputStream` 单线程
  3. 文件写盘单线程 `FileOutputStream` 串行
  4. `MAX_EXTRACT_BYTES = 512MB` 远超实际（66MB Piper 实际只有 8 倍余量）
  5. "统计文件数…"阶段无 ETA
- **修法**：
  1. **去掉预扫**，用 `modelInfo.sizeBytes * 1.5` 估算分母 → 立省 50% CPU
  2. bzip2 多线程（换 `aircompressor` / `jbzip2` 依赖）→ 提速 2-3×
  3. `BufferedOutputStream` + 8KB buf，espeak-ng-data 用 `RandomAccessFile` 预分配
  4. `MAX_EXTRACT_BYTES` 改 256MB
  5. UI 加 ETA 提示
- **涉及文件**：`tts/EmbeddedTtsEngine.kt:851-868, 262-266, 1015-1043`；`ui/screens/reader/ReaderViewModel.kt:295-298`

#### 2.7 [P1] TTS 状态写入耦合在 `speakMutex` 锁内（设计取舍）
- **文件**：`tts/EmbeddedTtsEngine.kt:1404-1417`
- **现象**：状态/锁耦合反模式，**界面/状态与实际播放脱节**。
- **修法**：把 `_state.value = EngineState.READY(...)` 移到 `speakMutex.withLock` 块**外**。
- **现状**：代码侧评估后保留锁内写入（有注释：避免 `READY ∧ tts=null` 说谎状态），如需采纳原修法再排期。

#### 3.6 [P1] 阅读页 朗读累计字数虚增（`nextParagraph`+300ms 防抖竞态）
- **文件**：`ui/screens/reader/ReaderViewModel.kt:1315-1348, 1629-1643`
- **修法**：`lastRecordedParagraphIndex` 更新放到 `_uiState.update` 时立刻执行（同步原子地），不要放到 300ms 后。

#### 5.3 [P1] 通知 权限被"don't ask again"后无引导
- **文件**：`ui/screens/settings/SettingsScreen.kt:663-671`
- **修法**：
  - `shouldShowRequestPermissionRationale` 区分"暂拒" vs "永久拒"
  - 暂拒：解释为什么需要
  - 永久拒：直接跳设置
  - 工具函数 `openAppNotificationSettings()` 跳 `Intent(Settings.ACTION_APP_NOTIFICATION_SETTINGS)`
  - 同步把 `notifications_enabled` 写 false

#### 5.4 [P1] 通知 channel 不可调整，无"通知偏好" UI
- **文件**：`util/NotificationHelper.kt:41-58`
- **修法**：
  - 设置页新增"通知偏好"卡片：复习提醒 / 下载进度 / 完成通知 各自开关 + 重要性选择
  - DataStore 持久化
  - 用户改动时**删除旧 channel + 重建**（channel 创建后重要性不可改）
  - "去系统通知设置"按钮

#### 8.5 [P1] 翻译 结果无持久化：DataStore 没有翻译缓存键
- **文件**：`data/repository/SettingsRepositoryImpl.kt:21-34`, `ui/screens/reader/ReaderViewModel.kt:686-689, 1511-1576`
- **修法**：
  - Room 加 `ParagraphTranslationEntity(bookId, paragraphIndex, sourceText, translatedText, langPair, translatedAt)` + DAO
  - `ReadingRepository` 加 `getTranslations(bookId, langPair)` / `saveTranslation(...)`
  - `translateAllParagraphs` 优先读 Room 缓存
  - `loadBook` 不再硬清 `paragraphTranslations`，改为从 Room 拉

#### 11.6 [P1] 复习 `isSubmitting` 在 `AnimatedContent` 退场期间释放 → 极快速点击旧按钮
- **文件**：`ui/screens/review/ReviewViewModel.kt:170`, `ui/screens/review/ReviewScreen.kt:358-423`
- **修法**：`isSubmitting` 在退场动画结束后释放：`withFrameNanos { _uiState.update { it.copy(isSubmitting = false) } }`

#### 12.3 [P1] 词典 远端 manifest 硬编码到作者私有 GitHub 仓库
- **文件**：`util/DictionaryManager.kt:64`, `app/build.gradle.kts`（加 buildConfigField）, `ui/screens/dictionary/DictionaryManagerScreen.kt:280-294`
- **现象**：词典管理页"刷新"按钮 100% 失败（jsDelivr 对私有仓返回 404）。
- **根因**：`MANIFEST_URL` 硬编码个人仓库路径，没通过 `BuildConfig` 或环境变量注入。
- **修法**：`BuildConfig` 注入 `DICTIONARY_MANIFEST_URL`；锁版本（tag 而非 branch）；公开 CDN 镜像 / 失败时降级。

### 2.3 P2（边界 / 性能）

#### 2.10 [P2] TTS `preprocessForTts` 缩写无上下文，`CT scan` 误读 "Central Time scan"
- **文件**：`tts/EmbeddedTtsEngine.kt:454-465`
- **修法**：高频歧义缩写（CT/PT/MT/ET）需要 POS tagger 配合（`PosTagger` 已 import 但未用）。

#### 5.5 [P2] 通知 `EmbeddedTtsEngine.showDownloadNotification` 用同一 channel 弹"下载中"通知
- **文件**：`tts/EmbeddedTtsEngine.kt:1867+`
- **现象**：复习提醒频道混着下载进度通知，IMPORTANCE_DEFAULT 会响铃震动。
- **修法**：跟 5.2 一起改，TTS 下载通知用独立 channel `eareye_tts_download`（IMPORTANCE_LOW，静默）。

#### 9.9 [P2] EPUB `BookEntity.filePath` 存绝对路径，换设备/清数据后失效
- **文件**：`data/local/entity/BookEntity.kt:21`, `ui/screens/library/LibraryViewModel.kt:283-323`, `util/EpubParser.kt`
- **修法**：EPUB 路径用 `content://` URI + `takePersistableUriPermission`；实体存 URI 而非绝对路径；解析时 `ContentResolver.openInputStream(uri)`。

#### 9.10 [P2] EPUB `MainActivity` 不处理 `Intent.ACTION_VIEW` 的 `application/epub+zip`
- **文件**：`AndroidManifest.xml:50-58`, `MainActivity.kt`
- **修法**：manifest 增加 `<intent-filter><action VIEW/><data mime="application/epub+zip"/></intent-filter>`；`MainActivity.onCreate` / `onNewIntent` 处理 `Intent.ACTION_VIEW`，跳转到 Library 触发导入。

#### 11.8 [P2] 复习 导出/导入备份漏 `review_records` → 恢复后 SM-2 进度全丢
- **文件**：`ui/screens/settings/SettingsScreen.kt:446-575`
- **修法**：导出加 `root.put("reviewRecords", ...)`，序列化每个 `ReviewRecordEntity`；导入插入每条 vocabulary 后按 `vocabularyId` 找新 id 并插入 `ReviewRecordEntity`（`OnConflictStrategy.IGNORE`）；snackbar 提示"已导入 X 条复习记录"。

#### 11.13 [P2] 复习 HomeViewModel `loadDataJob?.cancel()` 旧 job 的 combine flow 不停止 collect
- **文件**：`ui/screens/home/HomeViewModel.kt:76-105`
- **修法**：`loadDataJob?.cancelAndJoin()`；或在 `loadData` 内把 `combine` 放到 `loadDataJob = viewModelScope.launch { combine(...).collect() }` 中。

#### 11.16 [P2] 复习 Snackbar 同字符串去重问题
- **文件**：`ui/screens/library/LibraryScreen.kt:46-52` 等多处
- **修法**：ViewModel 侧 emit 加 `eventId: Long` 计数器。

### 2.4 P3（死代码 / 异味）

#### 11.18 [P3] 复习 `RecordReviewEntity.lastReviewDate` 与 `VocabularyEntity.lastReviewTime` 命名分叉
- **文件**：`data/local/entity/ReviewRecordEntity.kt:29`, `data/local/entity/VocabularyEntity.kt:26`
- **修法**：`ReviewRecordEntity.lastReviewDate` 改名为 `lastReviewedAt`。

#### 11.20 [P3] 复习 `getWordsByBook` 暴露但 UI 无调用 → "按书查词"功能未接通
- **文件**：`data/local/dao/VocabularyDao.kt:36-37`, `domain/repository/Repositories.kt`
- **修法**：`VocabularyRepository` 加 `getWordsByBook(bookId): Flow<List<Vocabulary>>`；阅读页/库详情页加入口。

#### 12.5 [P3] 词典 文件全量加载到 `LinkedHashMap`，多词典切换/大词典 OOM 风险
- **文件**：`util/DictionaryManager.kt:284-295, 325-338`
- **修法**：单文件 > 10MB 走 Room `DictionaryEntry` 表（word PK, definition TEXT）+ BTree 索引；或按首字母分片 + 内存 LRU。

### 2.5 产品决策（待拍板）

#### 4.1 [产品决策] 主题/外观 移除"外观"段（含深色模式 + Collins 高亮）
- **文件**：`ui/screens/settings/SettingsScreen.kt:965-990`
- **现象**：阅读主题（ReadingTheme LIGHT/DARK/SEPIA）与外观深色模式（`uiState.darkMode`）两套系统未联动，色温割裂。
- **决策**：直接把"外观"段（971-990 行的整个"外观"卡片）移除。App 主题完全由"阅读主题"控制。
  - `uiState.darkMode` 字段在 DataStore 里可保留兼容旧数据，但 UI 不再暴露
  - `viewModel.setDarkMode` 函数（422 行附近）保留兼容层（写 DataStore 但 UI 不调）或彻底删除
  - `MainActivity` 里基于 `darkMode` 选 `darkColorScheme`/`lightColorScheme` 的逻辑改为永远读 ReadingTheme
- **涉及文件**：
  - `ui/screens/settings/SettingsScreen.kt:965-990`（移除整段）
  - `ui/screens/settings/SettingsScreen.kt:422-425`（`setDarkMode` 兼容层决策）
  - `ui/screens/reader/ReaderScreen.kt:102-111`（阅读页背景渲染）
  - `ui/theme/Theme.kt`
  - `data/repository/SettingsRepositoryImpl.kt:89-91`

#### 4.3 [产品决策] 主题/外观 移除"高亮 Collins 等级"开关
- **文件**：`ui/screens/settings/SettingsScreen.kt:980-987`
- **现象**：默认开但与 Reader 端 `showWordLevelColors` 字段不同步，"打开但没反应"/"关不掉"。
- **根因**：Settings `collinsHighlight`（DataStore 持久化）与 Reader `showWordLevelColors`（VM 内存）两个字段互不感知。
- **决策**：跟 4.1 一起移除，Collins 词频颜色入口保留在阅读页内（`ReaderScreen.kt:1930-1936`），仍走 ReaderViewModel 内存的 `showWordLevelColors`。
- **涉及文件**：
  - `ui/screens/settings/SettingsScreen.kt:980-987`（移除）
  - `ui/screens/settings/SettingsScreen.kt:56, 178-186, 442-444`（可保留兼容层）
  - `data/repository/SettingsRepositoryImpl.kt:83-84, 99-101`

---

## 三、已修复问题（归档）

> 保留原编号；批次 = 首批/第二批（代码复核确认）/ 第三批（见第六节 6.2）/ 第四批（见第六节 6.1）。
> 9.2、9.7 为部分完成（遗留子项见"遗留"列）。

### 3.1 设置页 TTS 下载（2/3）

| 编号 | 批次 | 修法 |
|------|------|------|
| **1.1** | 首批/第二批 | 解压/初始化阶段不再硬编码"正在下载…请保持网络连接"，按阶段显示"正在解压/正在初始化"，subtitle 改"无需联网" |
| **1.2** | 首批/第二批 | 抽 `shouldCountTarEntry` 共用预扫与解压过滤口径，末态分子分母对齐，进度不再卡 99% |

### 3.2 TTS 引擎（10/12）

| 编号 | 批次 | 修法 |
|------|------|------|
| **2.1** | 首批/第二批 | AudioTrack buffer = `maxOf(pcm16.size * 2, getMinBufferSize())`，长句不再静默丢失 |
| **2.2** | 首批/第二批 | 货币正则改为 `\$(\\d+)?(?:\.(\\d{1,2}))?`，`$.50`/`$0.99` 正确读法 |
| **2.3** | 首批/第二批 | fullStream 取消探测改 `sinceCheck` 累加器，最后 256KB 也能响应取消 |
| **2.4** | 首批/第二批 | 取消 `conn?.inputStream != null` 副作用探测，改按 `responseCode` 判定 |
| **2.5** | 首批/第二批 | 以 `playbackHeadPosition` 判定播完再 release，句尾不再 100-200ms 截断 |
| **2.6** | 首批/第二批 | 重定向分支不再在 finally 双重 disconnect |
| **2.8** | 首批/第二批 | 句子链句间 `delay(20)`，消除 AudioTrack 切换"啵"声 |
| **2.9** | 首批/第二批 | CancellationException 路径调 `cleanExtractionPartials` 清 tarball 残片 |
| **2.11** | 第四批 | `speak()` 入口 `coroutineContext[Job]?.ensureActive()`，杜绝 TtsHelper cancel 后双 speak 并发 |
| **2.12** | 首批/第二批 | 单字母词 `w in collinsOneLower` 判 CORE，`"I"` 不再 UNKNOWN |

### 3.3 阅读页（6/10）

| 编号 | 批次 | 修法 |
|------|------|------|
| **3.1** | 首批/第二批 | loadBook 清空 `bookmarkedParagraphs`/`highlights`，幽灵书签消除 |
| **3.2** | 首批/第二批 | 换书取消 `selectWordJob`/`sentenceTranslateJob`；addToVocabulary 加 `myBookId` 快照 |
| **3.3** | 首批/第二批 | `revealed` 改 `rememberSaveable(paragraphs)`，不再串到新书 |
| **3.4** | 首批/第二批 | 朗读中句子也包进 `TappableParagraphText`，朗读时可点词查义 |
| **3.5** | 第三批 | 挖空隐藏词补 `.clickable { onReveal() }` + `padding(4dp, 2dp)` 扩大可点区 |
| **3.7** | 第五批 | `textLayoutResult` 改 `remember(paragraph)`，段滚出/滚回点击不再失效 |

### 3.4 主题/外观（1/3）

| 编号 | 批次 | 修法 |
|------|------|------|
| **4.2** | 第三批 | `level.ordinal + 1` → `level.level`，难度色不再越界错位 |

### 3.5 通知时机/重启（2/2）

| 编号 | 批次 | 修法 |
|------|------|------|
| **6.1** | 首批/第二批 | `onCreate` 改 `appScope.launch { syncReminderPrefsMirror() }`，不再主线程 runBlocking 读 DataStore |
| **6.2** | 第五批 | 时间/时区变化改 Application.onCreate 动态注册；manifest 移除 `TIME_SET`/`TIMEZONE_CHANGED` |

### 3.6 文章解析/RSS（5/5）

| 编号 | 批次 | 修法 |
|------|------|------|
| **7.1** | 首批/第二批 | `RssArticle` 加 `content` 字段存完整正文（独立于 description，不受 MAX_DESC 截断） |
| **7.2** | 首批/第二批 | `extractContent` 前剔除 aside/nav/figure/iframe/button/form 与 class 型噪音容器 |
| **7.3** | 首批/第二批 | `addArticleToLibrary` 优先用 `article.content`，空才 fallback 抓 link |
| **7.4** | 首批/第二批 | 策略 5 命中 body 前先剥 `<header>/<footer>/<nav>/<aside>` |
| **7.5** | 第四批 | `cleanText` 块级标签边界→空行、按空行自然分段，不再"每 4 句硬切" |

### 3.7 翻译（8/10）

| 编号 | 批次 | 修法 |
|------|------|------|
| **8.2** | 首批/第二批 | `close()` 接入 cleanup/onTerminate/onDestroy；init 失败 60s 重试窗口 |
| **8.3** | 首批/第二批 | 失败段不写 `""` 占位，回译/分栏"重试"按钮可达 |
| **8.4** | 第五批 | 全文翻译改 `coroutineScope { async(IO) ... }.awaitAll()` 并发化，整书翻译显著提速 |
| **8.6** | 第四批 | `findSentenceAtOffset` 句末标点加 CJK `。！？；` |
| **8.7** | 第三批 | 单词级 ≤20 字符先查本地词典命中即返回，不被 ML Kit 抢走 |
| **8.8** | 第五批 | 选句翻译先清旧译文再换标题，消除"新标题 + 旧译文"中间帧 |
| **8.9** | 首批/第二批 | `lookupLocalDict` 对含空格输入直接返回 null；未翻译显示"句子未翻译" |
| **8.10** | 第三批 | 关闭翻译时取消 `translationJob` + 清空译文，不再偷跑流量 |

### 3.8 EPUB 导入（8/10）

| 编号 | 批次 | 修法 | 遗留 |
|------|------|------|------|
| **9.1** | 第四批 | `parseBook` 返回 `ParsedBook`，从 OPF 提取 dc:title/creator/language 并填充导入 | - |
| **9.2** | 第四批 | `ParsedBook.wasTruncated` 检测 + 导入 WARN 日志 | `isTruncated`/`originalCharCount` 入库与卡片提示待排期 |
| **9.3** | 首批/第二批 | 引入 `EpubParseException` 分类（损坏/加密/空/缺 OPF/无正文），UI 按类型提示 | - |
| **9.4** | 首批/第二批 | spine 区分 `linear="no"` + 非正文 href 黑名单过滤 | - |
| **9.5** | 第四批 | 优先读 `META-INF/container.xml` 定位 OPF，失败回退 first .opf 并 WARN | - |
| **9.6** | 第四批 | 章节字节级读取 + 编码探测（BOM/meta charset/xml 声明），GBK 不再乱码 | - |
| **9.7** | 第四批 | `BookDao.findByFilePath` 同路径去重复用旧 id | `dc:identifier` 唯一索引待排期 |
| **9.8** | 第四批 | `ParsedBook.images` + `EpubParseException.ImageOnly` 图册识别 | - |

### 3.9 IO/性能/安全（11/11）

| 编号 | 批次 | 修法 |
|------|------|------|
| **10.1** | 第五批 | 下载加 `MAX_FILE_BYTES`/`MAX_TEXT_BYTES` 上限，`LimitInputStream` 限制读入，超限删 `.tmp` |
| **10.2** | 首批/第二批 | URL scheme 白名单（http/https）+ href 挡 data:/mailto:/file:/vbscript: + catch 兜底 |
| **10.3** | 第三批 | `MAX_ZIP_ENTRIES=5000` + `resolveEntry` 兜底每轮 ≤200 次，杜绝 O(spine×entries) 算法炸弹 |
| **10.4** | 第三批 | `MAX_EPUB_BYTES=200MB` 文件级字节上限 |
| **10.5** | 第三批 | `isHtmlContentType` 白名单校验，非 HTML/文本拒收、缺失放行 |
| **10.6** | 第三批 | `LIKE` 补 `ESCAPE '\'` + 调用方转义 `\ % _` + 搜索词截断 64 |
| **10.7** | 第五批 | deleteBook 文件清理失败补日志 + 越界删除明确告警（结合 11.19） |
| **10.8** | 第三批 | 短段过滤阈值 `>10` → `>3`，章标题不再被吞 |
| **10.9** | 第四批 | manifest 双正则 → 单遍 `LinkedHashMap`（先到者胜），id/href 属性顺序无关 |
| **10.10** | 第四批 | 同 7.5：cleanText 按空行自然分段 |
| **10.11** | 第四批 | `unescapeJson` 删死代码（无用的 `hex` 变量与条件） |

### 3.10 复习页（14/20）

| 编号 | 批次 | 修法 |
|------|------|------|
| **11.1** | 首批/第二批 | EF 夹 `[1.3, 2.5]` + interval 走 Double 钳制，杜绝整数溢出永久 due |
| **11.2** | 第四批 | "困难"按钮 `onAnswer(3)` → `onAnswer(2)`（SM-2 q=2 才是困难，重置 interval=1） |
| **11.3** | 首批/第二批 | `addToVocabulary` 拿到 id 后追加 `addWordToReview`，生词进入复习队列 |
| **11.4** | 第四批 | 完成页显示"还有 N 张待复习" + "继续复习" |
| **11.5** | 第五批 | `answerCard` 判 `card.vocabulary == null`：删孤儿 + 推进不评分，不挡答题 |
| **11.7** | 第五批 | `ReviewRecordDao.deleteByVocabularyIds` + 加载时 `withTransaction` 批量清理孤儿记录 |
| **11.9** | 首批/第二批 | `answerCard` 用同一份 state 快照 + 写库前索引越界复判 |
| **11.10** | 首批/第二批 | 加载中显示 `CircularProgressIndicator`，不再白屏 |
| **11.11** | 首批/第二批 | SM-2 公式无条件更新 EF（q<3 也衰减），与原始论文一致 |
| **11.12** | 第五批 | `items(uiState.statuses, key = { it.info.id })`（代码实态确认已内置） |
| **11.14** | 首批/第二批 | `LibraryViewModel.deleteBook` 补 try-catch，不再崩 app |
| **11.15** | 第五批 | 日期 `LaunchedEffect` + `delay(60_000)` 每分钟刷新（代码实态确认已内置） |
| **11.17** | 第五批 | import MIME 去掉 `*/*` |
| **11.19** | 第五批 | deleteBook 清理前 `booksDir.mkdirs()` + canonical 失败回退 absolutePath |

### 3.11 生词本/词频/词典（4/6）

| 编号 | 批次 | 修法 |
|------|------|------|
| **12.1** | 首批/第二批 | `deleteBook` 事务内追加 `vocabularyDao.deleteForBook` + `reviewRecordDao.deleteByBookId` |
| **12.2** | 首批/第二批 | `addBook` 解析后调 `wordFrequencyDao.insertAll`，词频表不再死代码 |
| **12.4** | 随 8.7 | 词典查询先查本地（见 8.7） |
| **12.6** | 第五批 | `getActiveDict` 读盘 try-catch `FileNotFoundException`，命中即清内存态回退内置词典 |

---

## 四、已审但**未发现**真 bug 的点（确认安全）

- `EpubParser` 不存在 zip slip —— 它从不把 `ZipEntry.name` 写盘，无路径穿越面
- `BookRepositoryImpl.deleteBook` canonical path 校验（L149-153）正确
- `DictionaryManager.safeDictFile` canonical path 校验（L192-201）正确
- DataStore 并发写 —— `DataStore<Preferences>.edit` 内部 `Mutex` 串行化，安全
- `ReviewRecordDao` / `BookmarkDao` 用 `OnConflictStrategy.IGNORE` + 唯一索引防 race
- `RssParser` `MAX_BODY = 10_000_000` 字节上限
- `EmbeddedTtsEngine` 没有 `MAX_EXTRACT_BYTES` 常量（用户记忆有误）—— grep 全文无匹配

---

## 五、涉及文件总索引

> 行号为整理时点的参考；部分已修复条目行号已随改动漂移，定位请以 grep 为准。

```
tts/EmbeddedTtsEngine.kt
  851-868      countTarEntries（预扫，1.2 已共用口径 / 1.3 待删）
  947-1056     解压主循环（1.2 已修, 1.3）
  1007-1012    解压过滤（1.2 已修）
  1381-1384    fullStream 取消探测（2.3 已修）
  1404-1417    状态写入在锁内（2.7 待定）
  1687-1726    playPcm AudioTrack（2.1/2.5 已修）
  1867+        showDownloadNotification（5.2, 5.5）
  262-266      MAX_EXTRACT_BYTES（1.3.4）

util/EpubParser.kt
  35-42, 51    解析失败吞错（9.3 已修）
  53-55        first .opf 启发式（9.5 已修）
  94-101       resolveEntry O 炸弹（10.3 已修）
  58, 78, 159-185 编码 / 裸 HTML（9.6/9.8 已修）
  131-134      spine linear=no（9.4 已修）
  136-154      manifest 双正则（10.9 已修）

util/RssParser.kt
  119-125      RssArticle 缺 content（7.1 已修）
  218-223, 357-361  applyField / appendDesc 截断（7.1 已修）

util/TranslationHelper.kt
  86-89        硬编码 EN→ZH（8.1）
  129-159, 162-166  本地词典优先级（8.7 已修）
  168-175      串行 await（8.4）
  182-190      lookupLocalDict 句子（8.9 已修）
  192-197      close() 无人调（8.2 已修）

util/CollinsClassifier.kt:2257    排除单字母词（2.12 已修）
util/TtsHelper.kt:138-139, 167-183  cancel/launch 竞态（2.11 已修, 2.8 已修）
util/DictionaryManager.kt
  64           manifest 硬编码（12.3）
  325-338      LinkedHashMap OOM（12.5）
  369-430      下载无字节上限（10.1）
  284-295      状态错位（12.6）

util/ArticleParser.kt
  46-73, 78-109  URL scheme 注入（10.2 已修）
  46-62        无 Content-Type 校验（10.5 已修）
  249-310      噪音元素未剔除（7.2/7.4 已修, 7.5 已修）

data/repository/SettingsRepositoryImpl.kt
  21-34        翻译无缓存键（8.5）
  89-91        setDarkMode（4.1 产品决策）
  93-97        setNotifications 同步镜像（5.3）
  99-101       setCollinsHighlight（4.3 产品决策）

data/repository/VocabularyRepositoryImpl.kt:55-79  词汇 → 复习（11.3 已修）
data/repository/BookRepositoryImpl.kt
  34, 142      wordFrequencyDao 只用删不用写（12.2 已修）
  53-91        addBook 缺 metadata 接收（9.1/9.7 已修）
  65-69        解析失败笼统 IOException（9.3 已修）
  75-83        截断后 totalWords 虚低（9.2 部分已修）
  132-160      deleteBook 漏 vocabulary/review（12.1 已修）
  145-159      booksDir canonical 失败（11.19）

data/local/entity/BookEntity.kt:21, 27   绝对路径、默认 en（9.1 已修, 9.9）
data/local/dao/BookDao.kt:21-22, 36     无去重、LIKE 注入（9.7/10.6 已修）
data/local/dao/VocabularyDao.kt:36-37   getWordsByBook 未接通（11.20）

ui/screens/settings/SettingsScreen.kt
  305-316      refreshCacheSize（产品决策：移除）
  422-425      setDarkMode（4.1 产品决策）
  442-444      setCollinsHighlight（4.3 产品决策）
  446-575      导出导入漏 review_records（11.8）
  584-599      clearCache（产品决策：移除）
  663-671      notificationPermissionLauncher（5.1, 5.3）
  905-912      底部 SettingRow 硬编码（1.1 已修）
  949-953      通知权限申请入口（5.1）
  965-990      "外观"段待移除（4.1 产品决策）
  1013         MIME */*（11.17）
  1015-1025    "清除缓存"待移除（产品决策）

ui/screens/reader/ReaderScreen.kt
  102-111      背景渲染（4.1 产品决策）
  113-270      Scaffold 屏占比（3.8）
  254-262      bottomBar ReadingBottomBar（3.8）
  269          横向 20dp padding（3.8）
  494-495      TtsInstallDialog fallback（1.1 已修）
  635, 921, 984  各模式 LazyColumn（3.8）
  663-748      朗读中不可点击（3.4 已修）
  1063-1071    挖空点击区（3.5 已修）
  1197         翻译重试按钮（8.3 已修）
  1463         revealed 串书（3.3 已修）
  1930-1936    Collins 词频色彩 Switch（4.3 产品决策）
  2169, 2184-2191  findSentenceAtOffset CJK（8.6 已修）
  2197-2227    TappableParagraphText 滚出失效（3.7）

ui/screens/reader/ReaderViewModel.kt
  286-289      collect 阶段映射（1.1 已修）
  618-626      loadBook 漏 selectWordJob 取消（3.2 已修）
  673-695      loadBook 漏清 bookmarks/highlights（3.1 已修）
  1315-1348    防抖字数虚增（3.6）
  1354-1363    sentenceTranslateJob（3.2/8.8）
  1390-1427    selectWord 硬编码 en→zh（8.1）
  1412         level ordinal+1 错位（4.2 已修）
  1429-1464    addToVocabulary 不入复习（11.3 已修）
  1511-1576    toggleTranslation / translateAllParagraphs（8.4/8.5/8.10 已修）
  1538-1543    翻译失败回译按钮不可达（8.3 已修）
  1612-1626    saveProgress（3.6）
  1629-1643    sessionCharsRead 累加（3.6）
  1710-1744    cleanup 阻塞同步 IO（3.10）
  1787-1809    选句翻译中间帧（8.8）
  1920         onCleared 调 cleanup（3.10）

ui/screens/review/ReviewViewModel.kt
  19, 117-122  SM-2 q 范围错位（11.2/11.11 已修）
  65, 196-208  loadDueReviews LIMIT 50 / restartSession（11.4 已修, 11.5）
  107-190      answerCard 索引越界 + 双重写入（11.9 已修）
  121, 131     EF 无上限 + 整数溢出（11.1 已修）

ui/screens/review/ReviewScreen.kt
  94-110       加载中白屏（11.10 已修）
  322-330, 392-419  评分按钮 orphan（11.5）
  358-423      isSubmitting 退场期释放（11.6）

ui/screens/dictionary/DictionaryManagerScreen.kt:270-278  items(count) 崩溃（11.12）

ui/screens/home/HomeScreen.kt:41       跨午夜日期不更新（11.15）
ui/screens/home/HomeViewModel.kt:76-105  loadDataJob combine（11.13）

ui/screens/library/LibraryViewModel.kt
  244, 317, 511  Book 构造不传 language（8.1）
  283-323       importBook（9.1/9.7/9.3/9.9）
  345-349       deleteBook 缺 try-catch（11.14 已修）
  436-474       fetchArticleLinks（7.1/7.3 已修）
  495-543       addArticleToLibrary 不用 content（7.1/7.3 已修）

ui/screens/library/LibraryScreen.kt:46-52  Snackbar 同串丢失（11.16）

ui/Navigation.kt:79-102      根 Scaffold contentWindowInsets（3.9）

MainActivity.kt
  25-39        enableEdgeToEdge + 引导缺申请（3.9, 5.1）
  onCreate     不处理 ACTION_VIEW（9.10）

EareyeReadingApp.kt
  22-27        onCreate 通知 channel
  41-58        runBlocking 读 DataStore（6.1 已修）

receiver/NotificationReceiver.kt:42-59   通知散落（5.2）
receiver/BootReceiver.kt:16-32           ACTION_TIME_SET 动态注册（6.2 已修）

AndroidManifest.xml
  8            POST_NOTIFICATIONS（OK）
  39-77        无 <service>（5.2）
  50-58        无 VIEW intent-filter（9.10）
  66-75        BootReceiver：TIME_SET/TIMEZONE 已移除（6.2 已修，改动态注册）

ui/theme/Theme.kt  MaterialTheme 切换逻辑（4.1 产品决策）
```

---

## 六、修复记录历史

> 按批次倒序排列，为历史留档。当前状态以第二/三节为准。
> 首批/第二批（2026-08-31 整理前已内置）：代码复核确认 34 项，文档当时未逐条回标，详见第三节"首批/第二批"行。

### 6.3 第五批（2026-09-01：并发翻译 / 下载上限 / 通知重排 / 复习孤儿清理 / 防御性 hardening）

> 主攻：全文翻译并发化、词典下载字节上限、时间/时区变化的动态重排、复习孤儿记录清理、
> 点词布局残留、字节/路径防御性加固。本批含 2 项（11.12/11.15）为代码实态复核确认已内置。
> 验证：`./gradlew :app:detekt` 通过；`:app:testDebugUnitTest` 97 项全绿；`:app:compileDebugKotlin` 成功。

| 编号 | 严重度 | 文件 | 修法 |
|------|-------|------|------|
| **8.4** | P1 | `util/TranslationHelper.kt` | `translateParagraphs` 改为 `coroutineScope { async(Dispatchers.IO) ... }.awaitAll()` 并发翻译每段；空段/失败段处理与 8.3 判定保持兼容 |
| **8.8** | P2 | `ui/screens/reader/ReaderViewModel.kt` | 选句翻译先置 `_sentenceTranslation.value = null` 再换 `_selectedSentence`，消除"新标题 + 旧译文"中间帧 |
| **6.2** | P0 | `EareyeReadingApp.kt` + `AndroidManifest.xml` + `receiver/BootReceiver.kt` | 时间/时区变化改在 Application.onCreate 动态注册（系统受保护广播不再投递给 manifest 静态接收器）；manifest 移除 `TIME_SET`/`TIMEZONE_CHANGED` |
| **10.1** | P1 | `util/DictionaryManager.kt` | 下载加 `MAX_FILE_BYTES=500MB`/`MAX_TEXT_BYTES=1MB`；`downloadFile` 按累计字节超限抛 IOException 删 `.tmp`；`downloadText` 用 `LimitInputStream` 限制读入 |
| **10.7** | P2 | `data/repository/BookRepositoryImpl.kt` | deleteBook 文件清理失败不再被吞：补日志 + 拒绝删除 books 目录外文件时明确告警 |
| **11.5** | P1 | `ui/screens/review/ReviewViewModel.kt` | `answerCard` 入口判 `card.vocabulary == null`：事务内删孤儿记录 + 推进 index 不评分 |
| **11.7** | P2 | `ui/screens/review/ReviewViewModel.kt` + `data/local/dao/ReviewRecordDao.kt` | `ReviewRecordDao` 增 `deleteByVocabularyIds(List<Long>)`；加载时收集 orphans `withTransaction` 批量清理 |
| **11.12** | P2 | `ui/screens/dictionary/DictionaryManagerScreen.kt` | 代码实态确认已用 `items(uiState.statuses, key = { it.info.id })`（文档此前漏标） |
| **11.15** | P2 | `ui/screens/home/HomeScreen.kt` | 代码实态确认已用 `LaunchedEffect` + `delay(60_000)` 每分钟刷新日期（文档此前漏标） |
| **11.17** | P3 | `ui/screens/settings/SettingsScreen.kt` | import 文件选择器 MIME 数组去掉 `*/*`，不再覆盖 `application/json`/`text/plain` |
| **11.19** | P3 | `data/repository/BookRepositoryImpl.kt` | deleteBook 清理前 `booksDir.mkdirs()`；canonical 解析失败回退 `absolutePath` 前缀比较 |
| **12.6** | P3 | `util/DictionaryManager.kt` | `getActiveDict` 读盘包 try-catch `FileNotFoundException`，命中即清内存态并回退内置词典 |
| **3.7** | P2 | `ui/screens/reader/ReaderScreen.kt` | `TappableParagraphText` 的 `textLayoutResult` 改 `remember(paragraph)`，避免 LazyColumn 复用实例残留旧布局 |

### 6.1 第四批（2026-09-01：EPUB 解析链路 + 文章分段 + 复习/朗读修缮）

> 主攻 EPUB 导入解析链路（元数据/container.xml/编码/去重/图册/截断标记）、文章自然段落分段、
> 复习评分档位与剩余量提示、选句翻译 CJK 边界、TTS 入口竞态。
> 验证：`./gradlew detekt` 通过；`:app:testDebugUnitTest` 97 项全绿；`:app:assembleDebug` 构建成功。

| 编号 | 严重度 | 文件 | 修法 |
|------|-------|------|------|
| **9.1** | P0 | `util/EpubParser.kt` + `data/repository/BookRepositoryImpl.kt` + `ui/screens/reader/ReaderViewModel.kt` | `parseBook` 改返回 `ParsedBook(title/author/language/paragraphs/wasTruncated/images)`，从 OPF `<dc:title>/<dc:creator>/<dc:language>` 提取元数据；导入按"显式传入 > OPF 解析 > 文件名/Unknown/en"填充 |
| **9.2** | P0 | `util/EpubParser.kt` + `data/repository/BookRepositoryImpl.kt` | `ParsedBook.wasTruncated` 置位 + addBook WARN 日志；DB 持久化字段留待后续 |
| **9.5** | P1 | `util/EpubParser.kt` | 优先读 `META-INF/container.xml` 定位 OPF，失败回退 first .opf 并 WARN |
| **9.6** | P1 | `util/EpubParser.kt` | 字节级读取 + 编码探测（BOM/meta charset/xml 声明），GBK 不再乱码 |
| **9.7** | P1 | `data/local/dao/BookDao.kt` + `data/repository/BookRepositoryImpl.kt` | `BookDao.findByFilePath` + addBook 同路径去重 |
| **9.8** | P2 | `util/EpubParser.kt` | `ParsedBook.images` + `EpubParseException.ImageOnly` 图册识别 |
| **10.9** | P2 | `util/EpubParser.kt` | manifest 单遍 `LinkedHashMap`（先到者胜），属性顺序无关 |
| **7.5 / 10.10** | P3/P2 | `util/ArticleParser.kt` | `cleanText` 块级边界→空行、按空行自然分段 |
| **10.11** | P3 | `util/ArticleParser.kt` | `unescapeJson` 删死代码 |
| **8.6** | P1 | `ui/screens/reader/ReaderScreen.kt` | `findSentenceAtOffset` 句末标点加 CJK `。！？；` |
| **2.11** | P2 | `tts/EmbeddedTtsEngine.kt` | `speak()` 入口 `ensureActive()`，杜绝双 speak 并发 |
| **11.2** | P0 | `ui/screens/review/ReviewScreen.kt` | "困难"按钮 `onAnswer(3)` → `onAnswer(2)` |
| **11.4** | P1 | `ui/screens/review/ReviewScreen.kt` | 完成页"还有 N 张待复习" + 继续复习 |

新增/调整测试：`EpubParserTest`（9.1/9.5/9.6/9.8/9.2/10.9 用例，适配 ParsedBook）、`ArticleParserTest`（7.5/7.2 用例，extractArticle 改 internal）。

### 6.2 第三批（2026-09-01：防御性安全 + 阅读/翻译逻辑修缮）

> 验证：`./gradlew detekt` 通过；`:app:testDebugUnitTest` 88 项全绿；`:app:assembleDebug` 构建成功。

| 编号 | 严重度 | 文件 | 修法 |
|------|-------|------|------|
| **4.2** | P1 | `ui/screens/reader/ReaderViewModel.kt:1420` | `level.ordinal + 1` → `level.level` |
| **8.7** | P1 | `util/TranslationHelper.kt:183-192` | 单词级 ≤20 字符先查本地词典命中即返回 |
| **8.10** | P2 | `ui/screens/reader/ReaderViewModel.kt:1530-1547` | 关闭翻译取消 `translationJob` + 清空译文 |
| **3.5** | P1 | `ui/screens/reader/ReaderScreen.kt:1057-1079` | 挖空隐藏词补 `.clickable { onReveal() }` + 扩大可点区 |
| **10.3** | P1 | `util/EpubParser.kt` | `MAX_ZIP_ENTRIES=5000` + 兜底后缀匹配 ≤200 次 |
| **10.4** | P1 | `util/EpubParser.kt` | `MAX_EPUB_BYTES=200MB` 文件上限 |
| **10.5** | P2 | `util/ArticleParser.kt` | `isHtmlContentType` Content-Type 白名单 |
| **10.6** | P2 | `data/local/dao/BookDao.kt` + `data/repository/BookRepositoryImpl.kt` | `LIKE` `ESCAPE '\'` + 调用方转义 + 截断 64 |
| **10.8** | P2 | `util/EpubParser.kt` | 短段过滤阈值 `>10` → `>3` |

新增/调整测试：`EpubParserTest`（10.4/10.3 上限防护 2 例）、`ArticleParserTest`（10.5 Content-Type 3 例）。
