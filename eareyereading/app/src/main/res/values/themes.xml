<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.EareyeReading" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Android 12+ Splash Screen Theme
         v2: 用独立 splash 图标 + 品牌暖白底（与 AppIcon 背景一致） -->
    <style name="Theme.EareyeReading.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FBF8F3</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.EareyeReading</item>
    </style>
</resources>
