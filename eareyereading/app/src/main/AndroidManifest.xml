<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Android 11+ 包可见性：不声明这些，queryIntentServices/getPackageInfo
         会被系统过滤成空结果，TTS 引擎探测（TtsEngineHelper）在现代设备上
         整个失效——而这恰是国产手机 TTS 兜底逻辑的目标场景 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <package android:name="com.google.android.tts" />
        <!-- Google Play 包名：hasGooglePlay() 探测（引导安装 TTS 引擎） -->
        <package android:name="com.android.vending" />
        <package android:name="com.xiaomi.mibrain.speech" />
        <package android:name="com.xiaomi.speech.tts" />
        <package android:name="com.huawei.hitouch.tts" />
        <package android:name="com.huawei.tts" />
        <package android:name="com.hicloud.android.tts" />
        <package android:name="com.coloros.tts" />
        <package android:name="com.oppo.tts" />
        <package android:name="com.vivo.tts" />
        <package android:name="com.baidu.duer.tts" />
        <package android:name="com.baidu.tts" />
        <package android:name="com.iflytek.tts" />
        <package android:name="com.iflytek.cloudtts" />
        <package android:name="com.tencent.tts" />
        <package android:name="com.sogou.tts" />
    </queries>

    <application
        android:name=".EareyeReadingApp"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EareyeReading">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.EareyeReading.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- issue 9.10：系统"打开方式"选择 .epub 直接进入并导入书库 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/epub+zip" />
            </intent-filter>
        </activity>

        <!-- 复习提醒闹钟接收器 -->
        <receiver
            android:name=".receiver.NotificationReceiver"
            android:exported="false" />

        <!-- 开机自启：重新调度闹钟（issue 6.2：TIME_SET/TIMEZONE_CHANGED
             已改为 EareyeReadingApp 动态注册，manifest 静态注册收不到） -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- 复习速览桌面小组件：待复习词数 + 连续打卡 -->
        <receiver
            android:name=".receiver.ReviewWidgetProvider"
            android:exported="false"
            android:label="复习速览">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/review_widget_info" />
        </receiver>

    </application>

</manifest>
