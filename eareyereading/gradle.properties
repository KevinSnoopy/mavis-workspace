org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
# Jetifier 已移除：全部依赖均为 AndroidX（kxml2/xmlpull 为纯 Java 测试依赖，
# 无需重写），省去每个第三方构件的重写开销
kotlin.code.style=official
android.nonTransitiveRClass=true
